# 多人端房间 UX 低保真设计

更新时间：2026-05-26

本文是 M3.2 房间 UX 设计稿，先定义信息结构、状态和安全口径。此阶段不实现页面代码；后续进入视觉稿或页面实现前，仍需按 Build Web Apps 流程产出确认版设计图并完成 Browser 验证。

## 1. 设计目标

- 普通用户只能看到“输入加入码”入口，不能从赛事 ID 反查裁判码、队长码或大屏码。
- 裁判创建赛事后只在确认页看到一次完整房间码，界面必须提醒“只显示一次”。
- 裁判后续进入房间管理页时，只能看到房间码是否已发放、队伍绑定和在线状态，不再显示明文码。
- 错误提示不泄漏有效角色、有效队伍数量或邀请码存在性。
- 蓝方/本地和红方/外地语义保留给后续赛事页面；加入房间阶段只强调身份和权限。

## 2. 入口结构

```text
┌────────────────────────────────────────────────────────────┐
│ HEXCORE 2.0 多人实时系统                                  │
│                                                            │
│ 加入赛事                                                   │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ 输入加入码                                           │   │
│ │ [ ref_xxxxx / cap_xxxxx / view_xxxxx / disp_xxxxx ]  │   │
│ └──────────────────────────────────────────────────────┘   │
│ [加入房间]                                                 │
│                                                            │
│ 连接状态：本地服务 / 局域网服务 / 云端服务                 │
└────────────────────────────────────────────────────────────┘
```

交互规则：

- 不提供“选择身份”按钮，身份由加入码在服务端解析。
- 加入失败统一提示“加入码无效或已失效”，不提示“这是裁判码但权限不足”等细节。
- 加入成功后根据服务端 session 跳转到裁判端、队长端、观众端或大屏端。

## 3. 创建赛事后的房间码确认页

```text
┌────────────────────────────────────────────────────────────┐
│ 房间已创建：S7 班德尔保卫战                                │
│ 重要：以下房间码只显示一次，请立即分发或导出。             │
│                                                            │
│ 裁判码                                                     │
│ [ ref_a8K... ] [复制]                                      │
│                                                            │
│ 观众码                                                     │
│ [ view_m2Q... ] [复制]                                     │
│                                                            │
│ 大屏码                                                     │
│ [ disp_P9x... ] [复制]                                     │
│                                                            │
│ 队长码                                                     │
│ ┌──────────┬──────────────┬──────────────┐                 │
│ │ 海斗1队  │ cap_3Ka...   │ [复制]       │                 │
│ │ 海斗2队  │ cap_N1b...   │ [复制]       │                 │
│ │ ...      │ ...          │ ...          │                 │
│ └──────────┴──────────────┴──────────────┘                 │
│                                                            │
│ [我已保存，进入裁判台]                                     │
└────────────────────────────────────────────────────────────┘
```

安全规则：

- 该页只使用创建赛事响应中的一次性 `room` 数据。
- 页面刷新后不再从 `/room` 拉取明文码。
- 复制操作只写前端剪贴板，不写日志明文。
- 进入裁判台前提醒裁判保存，避免后续无法查看明文码。

## 4. 裁判房间管理摘要

```text
┌────────────────────────────────────────────────────────────┐
│ 房间管理                                                   │
│ 赛事：S7 班德尔保卫战                                      │
│                                                            │
│ 凭据状态                                                   │
│ 裁判码：已发放                                             │
│ 观众码：已发放                                             │
│ 大屏码：已发放                                             │
│                                                            │
│ 队长入口                                                   │
│ ┌──────────┬──────────┬──────────┬──────────────┐          │
│ │ 队伍     │ 阵营     │ 码状态   │ 在线状态     │          │
│ │ 海斗1队  │ 本地     │ 已发放   │ 已连接       │          │
│ │ 海斗2队  │ 外地     │ 已发放   │ 未连接       │          │
│ └──────────┴──────────┴──────────┴──────────────┘          │
└────────────────────────────────────────────────────────────┘
```

安全规则：

- 该页调用 `GET /api/tournaments/:id/room?sessionToken=...`。
- 只有裁判、赛事管理员或超级管理员 session 可访问。
- 返回数据只包含 `codeIssued`、队伍 ID、队伍名和创建时间，不包含明文码或 hash。

## 5. 队长加入后的首屏

```text
┌────────────────────────────────────────────────────────────┐
│ 海斗1队 · 队长端                              已连接 v12   │
│ 当前阶段：等待裁判开始海克斯选择                           │
│                                                            │
│ 我的队伍                                                   │
│ 队长：小明                                                 │
│ 成员：待选 4 人                                            │
│                                                            │
│ 当前可操作                                                 │
│ 暂无操作，等待裁判推进。                                   │
└────────────────────────────────────────────────────────────┘
```

安全规则：

- 队长端只显示自己的队伍和公开流程信息。
- 不显示其它队伍隐藏商店、真实暗牌、房间码、裁判操作入口。

## 6. 观众和大屏加入后的首屏

观众端：

```text
┌────────────────────────────────────────────────────────────┐
│ HEXCORE 2.0 观众端                            实时同步中   │
│ 当前阶段：第 1 轮 / 海克斯选择                              │
│                                                            │
│ 队伍概览       顺位概览       事件流                       │
│ 只展示公开状态，不展示隐藏卡、房间码和裁判修正细节。       │
└────────────────────────────────────────────────────────────┘
```

大屏端：

```text
┌────────────────────────────────────────────────────────────┐
│                  HEXCORE 2.0                               │
│              S7 班德尔保卫战                               │
│                                                            │
│        本地阵营 0        vs        外地阵营 0               │
│                                                            │
│  当前事件：等待裁判开始                                    │
└────────────────────────────────────────────────────────────┘
```

## 7. 错误状态文案

| 场景 | 文案 |
| --- | --- |
| 加入码错误 | 加入码无效或已失效。 |
| 服务未连接 | 无法连接多人服务，请确认服务地址。 |
| 无权查看房间管理 | 当前身份无权查看房间管理。 |
| 会话过期 | 登录状态已失效，请重新输入加入码。 |
| 赛事不存在 | 未找到赛事，请检查服务地址或联系裁判。 |

## 8. 后续视觉稿要求

- 使用深色赛事控制台风格，信息密度高但不拥挤。
- 房间码确认页需要明显的“只显示一次”安全提示。
- 加入页不使用营销式 hero，不放无关说明。
- 按钮、输入框、状态点和表格行使用同一套组件。
- 所有文字在 1366px 宽度下必须不遮挡、不溢出。
- 移动端至少保证加入房间流程可用；裁判管理摘要可横向滚动或折叠。
