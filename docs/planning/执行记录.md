# 执行记录

说明：本文只记录实际执行过程。早期条目中的旧规则、旧远端和旧文档名称不再作为当前规范来源；当前待开发计划以 [当前待开发计划.md](当前待开发计划.md) 为准，规则规范以 [../product/阵营锁定10队金币商店模式实施规范.md](../product/阵营锁定10队金币商店模式实施规范.md) 为准，固定门禁见 [开发门禁.md](开发门禁.md)。

## 2026-05-30：v2.0.22 Win11 安装版端口占用提示

- 复现安装版启动失败：开发目录 `multiplayer-hexcore-1` 占用宿主机 `4186/4196`，安装目录 `hexcore2-hexcore-1` 无法绑定端口。
- 停止开发目录容器后，清理安装目录异常容器状态并重新启动，安装目录 `Start-HEXCORE2.ps1` 已成功启动 Docker PostgreSQL 版本。
- 启动脚本新增端口预检：若 `HEXCORE_APP_PORT` 或 `HEXCORE_API_PORT` 已被占用且当前安装目录 compose 服务未运行，会提示先关闭旧 HEXCORE2 或执行 `docker compose down`，避免直接暴露 Docker bind error。
- 将项目版本提升为 `2.0.22`，同步安装器版本、客户安装说明、部署运维说明、回归断言和 `CHANGELOG.md`。

## 2026-05-30：v2.0.21 Win11 安装版启动脚本修复

- 复现客户安装目录 `Start-HEXCORE2.ps1` 在 Windows PowerShell 5.1 下的解析失败，原因是 UTF-8 无 BOM 脚本中的中文字符串被本地代码页误读后破坏语法。
- 将 Windows 一键启动、停止、打开页面和日志脚本的运行时提示改为 ASCII 文案，保留脚本逻辑不变。
- 将项目版本提升为 `2.0.21`，同步运行界面版本元信息、安装器版本、客户安装说明、部署运维说明、回归断言和 `CHANGELOG.md`。
- 已将修复后的启动脚本同步到当前本机安装目录，便于立即重试。

## 2026-05-30：M13 Docker PostgreSQL 验收闭环与 v2.0.20

### 已完成

- 修复 Docker Desktop 安装残留目录 `C:\ProgramData\DockerDesktop` 所有者异常，将所有者改为 `BUILTIN\Administrators`，解除 Docker Desktop 安装阻断。
- Docker Desktop 已安装可用，Docker CLI 与 Compose 可执行。
- 修复多人端创建赛事前的赛事 ID 重复校验：改为读取 `/api/tournaments` 房间列表判断是否存在，避免用预期 404 的 `/snapshot` 请求污染浏览器 console。
- 在多人端 HTML 中增加空 favicon 声明，避免浏览器默认请求 `/favicon.ico` 产生 404 日志。
- 将项目版本提升为 `2.0.20`，同步 `package.json`、`package-lock.json`、运行界面版本元信息、安装器版本、客户安装说明、部署运维说明、回归断言和 `CHANGELOG.md`。
- M13 当前待开发计划状态更新为已完成，待随本轮提交推送 GitHub。

### 验证

- `npm run verify:docker-postgres` 通过，覆盖 `docker compose config`、`docker compose up -d --build`、`docker compose ps`、`/health.runtime.storage === postgres`、创建房间、裁判端、队长端、观众端、开店、购买、跳过、SSE 同步、容器重启恢复和 PostgreSQL 备份/恢复。
- Docker 容器状态：`multiplayer-hexcore-1` 与 `multiplayer-postgres-1` 均为 `healthy`。
- `/health` 返回 `runtime.storage = postgres`，并仅包含存储、房间数、session TTL、订阅数等运维信息。
- 本机 Edge DevTools 验证 `http://127.0.0.1:4186/`：
  - 页面非空，标题为 `HEXCORE 2.0 裁判控制台`。
  - 可创建 12 队无阵营房间。
  - 页面无错误覆盖层。
  - Network 和 console 均无 error/warn。
  - 截图保存在 `tmp/m13-browser-created-room-final.png`。

### 安全复核

- `.env.example` 可提交，真实 `.env`、数据库密码、备份 dump、Docker volume 数据和本机 `tmp/` 验收产物不进入 Git。
- 宝塔备份/状态脚本不 `source .env`，不通过 Docker CLI 参数传入 PostgreSQL 密码；备份在 `postgres` 容器内部读取容器环境变量执行。
- 安装器不内置 Docker Desktop，不写死正式数据库密码；首次启动脚本生成本机随机 PostgreSQL 密码。
- `/health` 不泄漏连接串、密码、房间码、`sessionToken` 或 `streamToken`。

## 2026-05-30：M13 Docker PostgreSQL 交付物第一阶段

### 已完成

- 将 M13 Docker PostgreSQL 验证与一键部署交付写入当前待开发计划，并把“多人端现场演示手册”从候选计划迁入 M13。
- 补齐 `.env.example` 前置模板，并调整 `.gitignore`、`.dockerignore`：真实 `.env` 继续忽略，模板允许提交，备份产物不进入 Git 或镜像上下文。
- 新增 Win11 一键脚本：
  - `scripts/windows/Start-HEXCORE2.ps1`
  - `scripts/windows/Stop-HEXCORE2.ps1`
  - `scripts/windows/Open-HEXCORE2.ps1`
  - `scripts/windows/Show-HEXCORE2-Logs.ps1`
- 新增 Inno Setup 安装器工程 `installer/windows/HEXCORE2_Setup.iss`，并成功编译 `output/HEXCORE2_Setup_v2.0.19.exe`。
- 新增宝塔 Docker PostgreSQL 部署脚本和 Nginx 模板：
  - `deploy/baota/install-hexcore-docker.sh`
  - `deploy/baota/update-hexcore.sh`
  - `deploy/baota/backup-postgres.sh`
  - `deploy/baota/status-hexcore.sh`
  - `deploy/baota/nginx-hexcore.conf`
- 新增 Win11 客户安装说明，并更新多人端部署运维说明、腾讯云宝塔大陆部署说明和文档索引。
- 新增 `scripts/verify-docker-postgres.js` 和 `npm run verify:docker-postgres`，用于 Docker 可用后自动验证 Compose、PostgreSQL、三端加入、开店、购买、跳过、SSE、容器重启恢复和备份/恢复。
- 差异范围安全复核后修正宝塔脚本：
  - 不再 `source .env` 执行环境文件内容。
  - 不再通过 Docker CLI 参数传入 PostgreSQL 密码。
  - 备份脚本改为在 `postgres` 容器内部读取容器环境变量执行 `pg_dump`。

### 验证

- `npm test` 通过，输出 `regression ok: 61 tests`。
- `git diff --check` 通过，仅有 Git 行尾转换提示。
- PowerShell 脚本 AST 解析通过。
- Inno Setup 6.7.3 编译通过，产物：`output/HEXCORE2_Setup_v2.0.19.exe`。
- Windows 已重启，WSL 前置条件已解除；但重启后当前 Codex 进程非管理员，通过 `winget` 和 UAC 启动尝试安装 Docker Desktop 仍以 `4294967291` 退出，安装日志显示 `Not run as admin`，当前 `docker` 命令仍不可用。
- `npm run verify:docker-postgres` 已补齐并尝试执行；脚本成功从 `.env.example` 生成本机 `.env`，随后在 `docker --version` 步骤失败，原因是本机无 Docker 命令。当前系统已重启，下一步需以管理员权限安装 Docker Desktop 后继续实机验收。

### 未完成 / 阻断

- Docker PostgreSQL 端到端验收尚未完成：
  - `docker compose config`
  - `docker compose up -d --build`
  - `docker compose ps`
  - `/health.runtime.storage === postgres`
  - 创建房间、裁判端、队长端、观众端、开店、购买、跳过、SSE 同步、容器重启恢复
  - PostgreSQL 备份/恢复实测
- `npm run verify:docker-postgres` 尚未通过实机执行。
- 由于 Docker 验收未完成，本阶段不能标记 M13 complete，不能按“标准验证通过之后上传 GitHub”的条件推送。

## 2026-05-30：多人端投影同步收口

### 已完成

- 修复多人端裁判投影缺口：新建无阵营 12 队房间后，前端会因服务端 `team-*` 队伍 ID 与本地默认 `c1-c10` 不一致而回落到 10 队默认画面。
- 服务端投影补充 `teamCount`、`totalTeams`、`playersPerTeam`、`campMode`、`pairingMode` 和 `allowSubstitutes`。
- 前端应用投影时会按服务端队伍列表替换本地队伍，并同步规则设置与顺位基础队列。
- 新增回归覆盖服务端动态规则投影和前端投影后 12 队无阵营赛程页。
- 将版本同步到 `v2.0.19`，并在 `CHANGELOG.md` 记录 M12 多规则多人端、多房间、Docker PostgreSQL 与投影同步收口。

### 验证

- `npm test` 通过，输出 `regression ok: 60 tests`。
- `node --check` 覆盖 24 个变更 JavaScript 文件，通过。
- `git diff --check` 通过，仅有 Git 行尾转换提示。
- `npm run post-task -- --doc=docs/planning/当前待开发计划.md --status=complete` 通过。
- 无头 Chrome 验证无阵营 12 队创建、进入裁判端和赛程页，未发现相关控制台异常；截图保存在 `C:\temp\hexcore-qa\m12-created-room.png`、`C:\temp\hexcore-qa\m12-referee.png`、`C:\temp\hexcore-qa\m12-schedule.png`。

## 2026-05-30：M12 多人端多规则与多房间收尾

### 已完成

- 多人端仅在 `apps/multiplayer`、`apps/server`、`packages` 和多人端相关文档/部署文件内继续实施。
- 补齐无阵营 `神兵天降` 服务端权威链路：
  - 购买成功后服务端生成响应窗口。
  - 队长端通过 `UseHexcore` command 发动，不依赖本地伪造结算。
  - 无阵营可夺取任意其他队长刚购买的公共池选手；原购买队长返还金币和购买权；发动者成功获得队员后下一轮跳过。
- 补齐无阵营赛程体验：
  - 无阵营模式默认生成全随机赛程。
  - 隐藏阵营对抗和班德尔保卫战入口。
  - 首轮槽位和待补齐提示改为左侧/右侧队伍，不再显示阵营 A/B、本地/外地槽位文案。
- 补齐替补激活/替换、多房间列表/归档/删除、Docker PostgreSQL 环境变量和部署文档的回归断言。

### 验证

- `npm test` 通过，输出 `regression ok: 60 tests`。
- 新增回归覆盖：
  - 无阵营 `神兵天降` 服务端响应窗口、夺取、退款、购买权返还、下一轮跳过和窗口关闭。
  - 无阵营赛程页隐藏阵营/班德尔入口，并默认生成随机赛程。

## 2026-05-29：M8.4/M8.5 宝塔同源部署适配

### 已完成

- `apps/multiplayer/src/main.js` 新增 API 默认地址推导：本地和局域网页面继续使用同主机 `4196`；公网域名或宝塔同源入口使用当前页面 origin。
- 加入房间和创建赛事共用默认 API 推导，不再在无输入时硬编码 `http://127.0.0.1:4196`。
- `apps/multiplayer/src/ui/referee-console.js` 的加入页服务地址直接读取统一默认值，便于 `https://hexsss.com` 下免手填端口。
- 新增 `docs/18_腾讯云宝塔大陆部署说明.md`，覆盖备案、DNS、安全组、SSL、Node/PM2、Nginx 反代、SQLite 持久化、验证清单、备份和回滚。
- `docs/17_多人端部署运维说明.md` 增加公网部署入口和同源反代说明。
- 继续完成 M8.6 第一版：房间码明文改为当前页面内存一次性展示，本地持久化状态清理旧 `createdRoom`；刷新后只显示安全摘要提示；创建赛事前会先校验服务端同名赛事是否已存在。
- 继续完成 M8.7 第一版：多人房间顶栏显示同步状态、服务端版本、最近同步和 session 剩余时间；队长关键 command 提交期间显示“提交中”并禁用开店、刷新、购买和跳过，避免重复点击。
- 继续完成 M8.8 第一版：服务端新增短期 `streamToken`，队长 SSE 订阅前通过 POST 换取短期凭据；前端事件流 URL 不再携带长期 `sessionToken`。
- M8.9 已拆分为 PostgreSQL schema/迁移、异步 store 边界、PostgreSQL store 适配器和备份恢复四段；当前同步 store 结构不直接塞入阻塞式 `psql` 命令，避免凭据暴露和事件循环阻塞。

### 验证

- 回归脚本增加同源 API 推导静态断言，后续由 `npm test`、`git diff --check` 和 `post-task` 门禁统一验证。
- 回归脚本增加房间码一次性展示、防旧码持久化和重复赛事 ID 预校验的静态断言。
- 回归脚本增加同步状态条、提交中状态和瞬态提交字段不落长期状态的静态断言。
- API 回归覆盖：无效 session 不能换取 `streamToken`，`stream-token` 接口不接受 URL 查询参数中的长期 `sessionToken`，队长 SSE 不再回退接受长期 `sessionToken` 查询参数；有效队长 session 可换取短期 token，队长 SSE 使用 `streamToken` 后仍只能看到本队赛程投影，返回内容不泄漏长期 sessionToken。
- Browser 验证：`http://127.0.0.1:4186/` 加入页非空；创建赛事后显示一次性房间码面板；用裁判码进入 `?role=referee` 后顶栏出现同步、版本、最近同步和会话剩余时间；console 无 error/warn。
- 截图证据保存在本机临时目录：`C:\Users\33664\AppData\Local\Temp\hexcore-qa\m8-join-gate.png`、`m8-created-room.png`、`m8-referee-sync.png`。
- 本轮未访问真实 `https://hexsss.com`，公网可用性需在服务器完成备案、DNS、SSL 和宝塔反代后现场验证。

### 安全复核

- 正式部署建议只开放 `80/443`，`4186/4196` 仅监听 `127.0.0.1` 并由 Nginx 转发。
- 房间码明文不再写入长期本地状态；当前改动不会把 sessionToken 放入下载文件。实时通道长期凭据治理已完成短期 `streamToken` 第一版，并收紧服务端避免队长 SSE 回退接受长期 URL 查询参数。
- 队长 SSE URL 已改为短期 `streamToken`，长期 `sessionToken` 仅用于 POST body、Authorization 或本机 localStorage 会话。后续正式化阶段仍需继续做 WebSocket 鉴权、token 轮换和代理日志策略。
- PostgreSQL 阶段安全复核结论：不采用同步 `psql` 子进程方案，不把数据库连接串写进命令行参数；下一步必须先抽象异步 store 初始化和方法调用，再接入 `pg` 或等价驱动。

## 2026-05-28：腾讯云宝塔大陆公网部署规划

### 已完成

- 用户明确服务器为腾讯云中国大陆，域名为 `hexsss.com`，运维入口为宝塔面板。
- 在 `docs/06_开发计划.md` 新增“腾讯云宝塔中国大陆公网方案”，将公网多人端目标细化为 `https://hexsss.com` 单入口、宝塔 Nginx 同源反代、ICP备案/公安备案、HTTPS、进程守护、持久化、房间分发、同步状态和公网安全升级。
- 在分阶段路线中新增 M8.4-M8.9：宝塔同源部署适配、大陆部署合规与运维手册、房间码与旧会话防误导、同步状态条与操作提交态、公网实时通道安全升级、PostgreSQL 正式持久化。
- 将公网部署分为 P0/P1/P2：P0 先支持跨省队长联调，P1 支持 10 队远程主持演练，P2 支持正式赛事级别的数据库、鉴权通道和灾备恢复。

### 验证

- 加载技能：使用 Build Web Apps 的前端产品方案视角细化队长端、裁判端和观众端可见体验；使用 Codex Security 的威胁建模视角补充公网令牌、房间码、HTTPS、备案、持久化和反向代理边界。
- 本轮只改开发计划和执行记录，没有改渲染代码；因此未启动 Browser 做页面截图验证。

### 安全复核

- 规划阶段识别的核心风险为：长期 sessionToken 出现在 SSE URL、旧房间码误导裁判、裸端口公网暴露、未备案域名不可正式访问、内存/SQLite 不适合正式长期赛事、备份与房间码下载文件不得泄漏 token。
- 已将对应修复路线写入 M8.4-M8.9，后续代码实现阶段必须做差异范围安全审查和 API 回归。

## 2026-05-28：M8.3 session 过期第一版

### 已完成

- `apps/server/memory-store.js` 为 session 增加 `issuedAt` 和 `expiresAt`，默认 TTL 为 24 小时，可通过 `HEXCORE_SESSION_TTL_HOURS` 或 `createServer({ sessionTtlMs })` 调整。
- `getSession()` 在 store 层统一清理并拒绝过期 session，覆盖队长投影、command、房间管理、审计和备份等读取路径。
- `apps/server/server.js` 与 `apps/server/sqlite-store.js` 透传 session TTL 配置；`/health` 输出 `sessionTtlSeconds`。
- 将项目版本提升为 `2.0.16`，同步 `package.json`、运行界面版本元信息、回归断言和 `CHANGELOG.md`。

### 验证

- 加载技能：本轮没有新增页面布局或视觉控件，未加载 Build Web Apps；改动涉及 session 和鉴权状态边界，按差异范围做安全复核并用回归固化。
- 回归新增 API 断言：短 TTL session 返回 `expiresAt`，过期后队长投影返回 401，command 写入返回 sessionToken 错误。

### 安全复核

- 重点检查过期 session 是否能继续读写、持久化是否保存 sessionToken 明文、健康检查是否泄漏 token；未发现新增可利用问题，剩余风险为仍未做凭据轮换和 HTTPS 反向代理配置。

## 2026-05-27：M5.6 权威经济结算第一版

### 已完成

- `apps/server/memory-store.js` 创建多人赛事时保留队伍经济状态和基础经济配置，默认初始金币为 6、刷新费用为 1/2/3/4。
- `packages/rules` 在服务端事件落地时维护队伍金币、刷新次数、购买权、跳过状态和下一队推进。
- `apps/server/shop-service.js` 由服务端计算刷新费用，队长 payload 不能伪造刷新费用或商店卡面。
- `apps/server/projections.js` 和 `apps/multiplayer/src/main.js` 同步公开队伍金币、购买扣费和最近购买结果。
- 将项目版本提升为 `2.0.15`，同步 `package.json`、运行界面版本元信息、回归断言和 `CHANGELOG.md`。

### 验证

- 加载技能：本轮没有新增页面布局或视觉控件，未加载 Build Web Apps；改动涉及服务端状态归一化、公开投影和 command 结算边界，按差异范围做安全复核并用回归固化。
- 回归新增 API 断言：服务端刷新商店扣除 1 金币并推进刷新次数；购买服务端卡位按价格扣费并同步 `lastPurchase.pricePaid/goldAfter`；金币不足刷新返回 400 且不推进版本；跳过本轮清空商店并推进到下一队。

### 安全复核

- 重点检查队长 payload 是否能伪造刷新费用、购买价格、队伍金币或公开投影字段；当前结算从服务端 snapshot 的商店卡位和经济配置推导，公开投影只输出金币余额、公开商店和轮内状态，不返回完整私有选手池。

## 2026-05-27：M8 部署运维第一版

### 已完成

- `scripts/start-multiplayer-stack.js` 新增多人端页面 + API 同启脚本，并在 `package.json` 暴露 `npm run start:multiplayer:stack`。
- `scripts/serve-multiplayer.js` 支持 `MULTIPLAYER_APP_PORT`，便于页面端口和 API 端口分开配置。
- `apps/server/server.js` 的 `/health` 增加启动时间、运行秒数和内存 store 运行统计。
- `apps/server/memory-store.js` 新增裁判/管理员专用权威备份导出能力，导出包含备份版本、时间、校验和和完整赛事状态。
- 新增 `docs/17_多人端部署运维说明.md`，记录本地/局域网启动、环境变量、健康检查、备份导出和当前限制。
- 将项目版本提升为 `2.0.14`，同步 `package.json`、运行界面版本元信息、回归断言和 `CHANGELOG.md`。

### 验证

- 加载技能：本轮没有新增页面布局或视觉控件，未加载 Build Web Apps；改动涉及部署脚本、健康检查和管理导出接口，按差异范围做安全复核并用回归固化。
- 回归新增 API 断言：`/health` 返回基础运行状态；匿名和观众不能导出赛事备份；导出接口不接受 URL 查询参数传 `sessionToken`；裁判通过 `Authorization: Bearer ...` 可导出带校验和的权威备份。
- 回归新增脚本断言：堆栈启动脚本通过 `spawn(process.execPath, ...)` 启动，不使用 shell 拼接命令。

### 安全复核

- 重点检查导出接口是否复用裁判/管理员 session 校验、健康检查是否泄漏私有状态、启动脚本是否存在命令注入；已确认导出接口匿名 401、观众 403，且不接受 URL 查询参数传令牌，健康检查只输出计数和运行状态，启动脚本不使用 shell。

## 2026-05-27：M7 队长赛程只读投影

### 已完成

- `apps/server/memory-store.js` 创建多人房间时保留归一化后的赛程快照，供后续投影裁剪使用。
- `apps/server/projections.js` 新增队长赛程投影：只返回当前队长相关场次，不返回无关队伍赛程或导入中的私有字段。
- `apps/server/server.js` 收紧 `view=captain` 的 `/projection` 与 `/events`：必须通过有效队长 `sessionToken` 绑定队伍视角。
- `apps/multiplayer/src/main.js` 接入服务端赛程投影，队长端“我的赛程”继续复用现有只读页面。
- 将项目版本提升为 `2.0.13`，同步 `package.json`、运行界面版本元信息、回归断言和 `CHANGELOG.md`。

### 验证

- 加载技能：本轮没有新增页面布局或视觉控件，未加载 Build Web Apps；改动涉及服务端投影、session 绑定和公开数据裁剪，按差异范围做安全复核并用回归固化。
- 回归新增 API 断言：匿名 `view=captain` 被拒绝；队长投影和队长 SSE 只包含本队相关场次；公开快照和观众投影不返回完整赛程。
- 前端回归断言确认 `applyRoomProjection()` 会同步 `snapshot.tournament`，队长 SSE 会携带 `sessionToken` 让服务端绑定本队视角。

### 安全复核

- 重点检查队长赛程是否依赖服务端 session 绑定而不是 URL 自报队伍、公开/观众投影是否泄漏完整赛程、队长投影是否带出无关场次或私有字段；未发现新增可利用问题。

## 2026-05-27：M6.3 回滚检查点与裁判回滚命令

### 已完成

- `packages/shared` 新增 `RollbackToVersion` command 和 `StateRolledBack` event，仅裁判/管理员可执行。
- `packages/rules` 为每个权威版本保存内部检查点；回滚时恢复目标版本快照，生成公开 `lastRollback` 提示，并把回滚动作写入审计摘要。
- `apps/server/projections.js` 新增 `lastRollback` 公开投影，并为回滚事件输出目标版本字段。
- 将项目版本提升为 `2.0.12`，同步 `package.json`、运行界面版本元信息、回归断言和 `CHANGELOG.md`。

### 验证

- 加载技能：本轮没有新增页面布局或视觉控件，未加载 Build Web Apps；改动涉及服务端回滚、状态归一化和公开投影边界，按差异范围做安全复核并用回归固化。
- 回归新增 API 断言：裁判可从版本 9 回滚到版本 8；回滚后公开快照包含 `lastRollback`，被回滚掉的裁判公告不再出现在观众投影；无效目标版本会被拒绝。
- 回归确认回滚动作进入裁判审计摘要，公开响应不包含 `auditLog`、私有检查点或 payload 额外字段。
- 回归确认队长和观众即使构造 `RollbackToVersion`，也会被服务端角色绑定拒绝。

### 安全复核

- 重点检查回滚是否只能由裁判/管理员执行、目标版本是否必须早于当前版本、公开投影是否泄漏内部检查点或完整私有选手池；已确认回滚只使用服务端内部检查点，公开侧只输出回滚原因和目标版本。

## 2026-05-27：M6.2 强制裁决公告最小闭环

### 已完成

- `packages/rules` 的 `RefereeRulingForced` 事件现在会在权威快照写入 `lastRefereeRuling`，记录裁判公告原因、摘要、事件序号和时间。
- `apps/server/projections.js` 新增 `lastRefereeRuling` 公开投影，让队长端和观众端能看到最近一次裁判强制裁决公告。
- 将项目版本提升为 `2.0.11`，同步 `package.json`、运行界面版本元信息、回归断言和 `CHANGELOG.md`。

### 验证

- 加载技能：本轮没有新增页面布局或视觉控件，未加载 Build Web Apps；改动涉及服务端公开投影边界，按差异范围做安全复核并用回归固化。
- 回归新增 API 断言：裁判提交 `ForceRefereeRuling` 后，公开快照包含裁决原因和摘要，观众投影可读到裁决公告；payload 里额外塞入的隐藏随机字段和选手列表不会进入公开响应。
- 回归确认强制裁决仍进入裁判审计摘要。

### 安全复核

- 重点检查 `ForceRefereeRuling` 是否开放任意状态 patch、公开投影是否泄漏 payload 额外字段、审计摘要是否保留裁决记录；已确认第一版只公开 `reason` 和 `patchSummary`，不执行任意快照改写。

## 2026-05-27：M6.1 暂停/恢复审计最小闭环

### 已完成

- `packages/rules` 为 `StateImported`、`TournamentPaused`、`TournamentResumed`、`RefereeRulingForced` 和 `MatchScoreRecorded` 生成高影响动作审计摘要，记录事件序号、版本、操作者、命令类型、原因和裁决摘要等 allowlist 字段。
- `apps/server/memory-store.js` 新增裁判/管理员会话校验复用方法，并提供 `getAuditLog()`。
- `apps/server/server.js` 新增 `GET /api/tournaments/:id/audit`，只允许裁判/管理员查看审计日志。
- 将项目版本提升为 `2.0.10`，同步 `package.json`、运行界面版本元信息、回归断言和 `CHANGELOG.md`。

### 验证

- 加载技能：本轮没有新增页面布局或视觉控件，未加载 Build Web Apps；改动涉及服务端鉴权、审计和公开投影边界，按差异范围做安全复核并用回归固化。
- 回归新增 API 断言：裁判暂停会推进 `paused` 状态但公开投影不暴露 `auditLog`；匿名访问审计接口返回 401，观众访问返回 403，裁判可读取高影响动作摘要且不包含 `sessionToken`。
- 回归新增暂停态断言：赛事暂停后，队长端普通开店动作被服务端拒绝；裁判恢复后 `paused` 解除并写入审计摘要。

### 安全复核

- 重点检查审计接口是否复用裁判/管理员 session 校验、公开快照是否泄漏审计日志、审计摘要是否回传 sessionToken；已确认审计接口不可匿名/观众读取，公开投影只同步 `paused` 状态，不直接输出 `auditLog`。

## 2026-05-27：M5.5 权威购买归属最小闭环

### 已完成

- `packages/rules` 的 `ShopCardPurchased` 在标记卡位已购买后，会把服务端当前商店槽位中的真实选手写入对应队伍成员列表，并将私有 `players` 中该选手标记为 `drafted`、绑定 `teamId`。
- `apps/server/projections.js` 的公开队伍投影新增成员 ID 列表；完整私有 `players` 仍不进入公开快照。
- 多人端前端 `applyTeamProjection()` 接收公开成员列表后，会同步本地 `captain.team`，并更新已有可见选手的 `status` 与 `teamId`。
- 将项目版本提升为 `2.0.9`，同步 `package.json`、运行界面版本元信息、回归断言和 `CHANGELOG.md`。

### 验证

- 加载技能：本轮没有新增页面布局或视觉控件，未加载 Build Web Apps；改动涉及服务端状态归属和公开投影边界，按差异范围做安全复核并用回归固化。
- 回归新增 API 断言：购买服务端生成商店卡后，公开队伍成员包含被购买选手，商店卡位为已购买，`lastPurchase` 使用服务端真实槽位选手，公开快照不包含完整私有 `players` 或伪造 `playerId`。
- 回归新增前端同步断言：队伍投影会写入 `captain.team`，并将本地可见选手标记为 `drafted`。

### 安全复核

- 重点检查购买归属是否仍从服务端商店槽位推导、公开队伍投影是否泄漏完整私有选手池、前端是否只应用服务端投影；已确认客户端 payload 中的 `playerId` 不参与归属结算，公开快照只输出队伍成员 ID、当前商店和最近购买结果。

## 2026-05-27：M5.4 权威商店生成最小闭环

### 已完成

- 修正 `packages/rules` 中事件 payload 的元信息覆盖顺序，`commandType` / `commandRole` 由服务端最终写入，客户端 payload 不能伪造为裁判角色。
- 新增 `apps/server/shop-service.js`：队长开店/刷新时，服务端可从赛事私有 `players` 选手池中按队伍阵营、可用状态和队长保护生成最多 5 张商店卡。
- `apps/server/memory-store.js` 支持创建赛事时保存私有选手池和队伍阵营；公开投影仍不返回完整 `players`。
- `apps/server/projections.js` 扩展公开商店卡字段，允许输出当前商店可见选手的名称、游戏 ID、位置、评分和英雄；被打乱商店仍按可见身份输出。
- 多人端前端 `applyCurrentShopProjection()` 会把服务端可见卡资料补入本地 `players`，从而复用原裁判端商店卡 UI。
- 将项目版本提升为 `2.0.8`，同步 `package.json`、运行界面版本元信息、回归断言和 `CHANGELOG.md`。

### 验证

- 加载技能：本轮没有新增页面布局或视觉控件，未加载 Build Web Apps；改动涉及鉴权/投影/服务端生成边界，按差异范围做安全复核并用回归固化。
- 回归新增 API 断言：创建赛事可保存私有选手池但公开快照不泄漏选手清单；队长开店会由服务端生成 5 张同阵营卡；伪造 `currentShop`、`commandRole` 和内部信任标记不会污染公开商店。
- 回归新增前端同步断言：`applyRoomProjection()` 应调用 `applyProjectedShopPlayers()`，保证服务端可见卡资料能在本地渲染。

### 安全复核

- 重点检查 command 元信息、内部 `_serverGeneratedProjection` 标记、公开投影和选手池可见范围；已确认客户端 payload 不能覆盖 `commandRole`，服务端会剥离客户端传入的内部信任标记，公开快照不输出完整私有选手池。

## 2026-05-27：M5.3 权威购买结果

### 已完成

- `packages/rules` 的 `ShopCardPurchased` 结算改为从当前权威 `currentShop.cards` 槽位读取购买选手，再标记卡位已购买和写入 `lastPurchase`；队长端 command payload 中的 `playerId` / `displayPlayerId` 不再作为购买结果来源。
- `apps/server/projections.js` 新增安全版 `lastPurchase` 投影，只输出当前可见选手标识；被打乱商店中真实暗牌仍不会进入公开快照。
- `apps/server/server.js` 的 command API 返回公开事件投影，不再把原始 command payload 直接回传给客户端。
- 多人端前端 `applyRoomProjection()` 接入 `lastPurchase`，收到 command 返回或 SSE 快照后可同步最近购买结果。
- 将项目版本提升为 `2.0.7`，同步 `package.json`、运行界面版本元信息、回归断言和 `CHANGELOG.md`。

### 验证

- 加载技能：本轮未改动页面布局、视觉设计或新交互，未加载 Build Web Apps；安全复核按差异范围执行，未触发全量 Codex Security 扫描。
- 回归新增规则包断言：队长购买裁判可信商店卡时，即使 payload 伪造 `playerId`，权威快照也会以服务端商店槽位中的选手为准，并消耗本轮购买权。
- 回归新增 API 断言：购买可信商店卡后公开快照显示卡位已购买、`lastPurchase` 使用可见选手 ID，响应正文不包含真实暗牌 ID 或伪造选手 ID。
- 本轮改动集中在服务端规则、投影和前端同步桥，没有新增页面布局或交互控件；未使用 Browser 验证，改由 `npm test` 和投影泄漏断言覆盖。

### 安全复核

- 本轮涉及鉴权和公开投影边界，差异范围重点检查 command 原始 payload、公开事件投影、`lastPurchase` allowlist 和暗牌字段；已确认队长端不能通过 payload 伪造购买选手，公开响应不输出房间码、sessionToken、真实暗牌或伪造 `playerId`。

## 2026-05-27：M5.2 权威投影扩展

### 已完成

- 服务端权威快照新增公开同步字段：
  - `currentShop`：当前公开商店、卡槽、购买状态和可见选手标识。
  - `roundStates`：各队各轮的免费开店、刷新次数、购买权和跳过状态。
  - `hexcoreActionWindows`：可公开的海克斯响应窗口。
- `packages/rules` 在应用 `ShopOpened`、`ShopRefreshed`、`ShopCardPurchased`、`TurnSkipped`、`HexcoreUsed` 事件时同步更新上述快照字段。
- `apps/server/projections.js` 对公开商店做 allowlist 投影；雪定饿的喵等显示身份与真实身份不一致时，只输出当前可见选手 ID，不输出真实暗牌 ID。
- 队长端 command 中携带的完整商店卡和海克斯窗口投影会被忽略，避免队长通过 payload 伪造服务端权威视图；完整投影只接受裁判/管理员可信动作或后续服务端规则生成结果。
- 多人端前端 `applyRoomProjection()` 接入公开商店、轮内状态和海克斯窗口应用，收到 command 返回或 SSE 快照后可更新本地视图。
- 将项目版本提升为 `2.0.6`，同步 `package.json`、运行界面版本元信息、回归断言和 `CHANGELOG.md`。

### 验证

- 回归新增服务端投影断言：
  - 开店事件会把商店、轮内状态和海克斯窗口落入权威快照。
  - API 返回的公开投影包含 `currentShop`、`roundStates` 和 `hexcoreActionWindows`。
  - 队长端伪造 `currentShop.cards` 和 `hexcoreActionWindows` 会被服务端忽略。
  - 被打乱商店中的真实暗牌 ID 不出现在公开投影中。

### 安全复核

- 本轮涉及服务端公开投影和实时同步边界，差异范围重点检查投影 allowlist、暗牌字段、队长伪造投影和 session/角色预检路径；已收紧为队长端不能通过 payload 伪造商店卡或海克斯窗口，未放宽原有 command 鉴权，公开投影不输出房间码、sessionToken 或真实暗牌 ID。

## 2026-05-26：M4 只读实时投影

### 已完成

- 新增 `apps/server/projections.js`：
  - 定义 `viewer`、`display`、`public` 只读投影视图。
  - 快照字段和事件 payload 字段统一使用 allowlist。
  - 大屏投影允许展示 `displayTitle`，但仍只来自公开字段。
- `apps/server/server.js` 调整：
  - `publicSnapshot()` 改为使用只读投影。
  - 新增 `GET /api/tournaments/:id/projection?view=viewer|display`。
  - SSE `/events?view=viewer|display` 按视图投影事件后再下发。
  - 非只读视图参数例如 `view=referee` 会被拒绝。
- `apps/server/memory-store.js` 调整 SSE 订阅结构，允许每个订阅者绑定自己的事件投影函数。

### 验证

- 回归覆盖：
  - 观众投影接口返回 `viewer` 视图。
  - 允许公开的 `summary` 字段保留。
  - `refereeCode`、真实选手 ID、内部随机种子不会出现在观众投影 payload。
  - `view=referee` 被拒绝，避免只读投影接口被误用成裁判视图。
- 本轮执行 `npm test` 后以实际门禁记录为准。

### 安全结论

- 观众端和大屏端后续应只接入投影接口，不读取裁判完整快照再由前端隐藏。
- 当前仍是 SSE 第一版，未做 WebSocket 重连补偿、事件缺口补齐和在线状态，这些保留到后续实时协议阶段。

## 2026-05-26：M3.2 房间 UX 低保真设计

### 已完成

- 新增 `docs/16_多人端房间UX设计.md`。
- 按安全加固后的产品口径设计房间 UX：
  - 普通用户只输入加入码，不手动选择身份。
  - 创建赛事后显示一次完整房间码，并提示裁判立即保存。
  - 裁判房间管理摘要只显示 `已发放` 和在线状态，不显示明文码或 code hash。
  - 队长端只显示自己队伍和当前可操作状态。
  - 观众端和大屏端只展示公开投影信息。
- 补充错误状态文案，避免提示泄漏邀请码是否存在、角色是否有效或队伍绑定细节。

### 验证

- 本轮是低保真结构设计，不写页面代码，不涉及浏览器渲染验证。
- 后续进入视觉稿和页面实现时，必须使用 Build Web Apps 产出确认版设计图，并使用内置 Browser 验证页面身份、非空、console、截图和关键交互。

## 2026-05-26：M3.1 房间安全加固

### 已完成

- `apps/server/memory-store.js` 调整房间凭据模型：
  - 默认裁判码、观众码、大屏码、队长码使用 `crypto.randomBytes` 生成。
  - `sessionToken` 使用 `crypto.randomBytes`，`actorId` 使用 `crypto.randomUUID`。
  - 服务端只保存房间码和 sessionToken 的 SHA-256 摘要。
  - 创建赛事响应一次性返回初始房间码，之后不再从服务端管理接口回传明文码或摘要。
- `apps/server/server.js` 调整接口安全边界：
  - `POST /api/tournaments` 返回 `{ tournament, room }`，其中 `room` 是一次性初始房间码。
  - `GET /api/tournaments/:id/room` 需要裁判/管理员 sessionToken，可通过 query 或 `Authorization: Bearer` 传入。
  - 未授权读取房间码管理信息返回 401，非管理身份返回 403。
  - 房间码管理接口只返回 `codeIssued` 等元信息，不返回明文码或 code hash。
- 保留并收敛公开投影 helper：公开快照和 SSE 事件通过 allowlist 输出，避免后续观众/大屏端依赖前端隐藏敏感字段。

### 验证

- `scripts/regression.js` 增加 M3.1 回归：
  - 创建赛事响应一次性返回初始房间码。
  - 匿名访问 `/room` 返回 401。
  - 队长、观众、裁判可分别使用创建时返回的加入码进入房间。
  - 裁判 session 可读取房间管理摘要，但摘要不包含明文码或 code hash。
  - 客户端不会收到 `sessionTokenHash`，sessionToken 不再使用 `session-...时间戳` 旧格式。
  - 旧攻击路径被拦截：匿名读取 `/room` 失败，猜测旧默认 `risk-demo-referee` 裁判码失败。
- 本轮执行 `npm test` 通过，输出 `regression ok: 52 tests`。

### 安全结论

- 已复现风险“匿名读 `/room` -> 获取 `refereeCode` -> 加入裁判 -> 提交裁判命令”在当前实现中失败。
- 当前仍是内存临时身份方案，尚未接入真实账号、密码、数据库、过期时间和凭据轮换；这些内容保留到后续账号/数据库阶段继续加固。

## 2026-05-26：Codex Security + Build Web Apps 联合优化多人端开发计划

### 已完成

- 按 Codex Security 审查结果调整多人端实施顺序：
  - M3 房间加入骨架保留为第一版能力，但不再视为可继续扩展 UI 的安全完成态。
  - 新增 M3.1 房间安全加固阶段，要求先修复匿名 `/room` 泄漏邀请码、临时凭据弱随机和明文凭据暴露风险。
  - 明确 M3.1 未完成前，不继续开发队长端、观众端、大屏端 UI。
- 按 Build Web Apps 工作流补充 UI 开发门禁：
  - 新界面实现前必须先产出设计图或低保真结构图并经确认。
  - UI 实现后必须使用内置 Browser 验证页面身份、非空页面、错误覆盖层、console、截图和关键交互。
  - 观众端、大屏端、队长端拆成设计阶段和实现阶段，避免未确认 UI 直接进入代码。
- 在 `docs/06_开发计划.md` 新增“Codex Security + Build Web Apps 联合门禁”，把安全复测、投影隔离、设计一致性和浏览器证据纳入验收口径。

### 安全结论

- 已复现的旧风险路径必须成为 M3.1 回归：未授权用户访问 `GET /api/tournaments/:id/room` 获取 `refereeCode`，再调用 `/join` 得到裁判 session，并提交 `PauseTournament` 成功。
- 后续修复完成前，相关接口不得作为 UI 接入基础。

### 前端结论

- 多人端 UI 开发顺序调整为：先安全口径，再设计图，再实现页面，再 Browser 验证。
- 任何未确认的临时 UI 页面不纳入提交。

## 2026-05-26：M3 临时房间加入与角色绑定

### 已完成

- `apps/server/memory-store.js` 新增房间访问码和 session 管理：
  - 创建赛事时生成裁判码、观众码、大屏码和队长码。
  - 支持传入 `teams` 自定义队伍 ID、队伍名和队长码。
  - 用户通过加入码生成 sessionToken，并绑定 `actorId`、`role`、`teamId`。
- `apps/server/server.js` 新增接口：
  - `GET /api/tournaments/:id/room`：读取当前赛事房间码和队伍码。
  - `POST /api/tournaments/:id/join`：使用房间码加入并返回 session。
- command API 改为使用 `sessionToken` 解析服务端保存的角色绑定，不再接受客户端直接传 `roleBinding` 冒充身份。

### 验证

- 回归测试新增/调整 M3 覆盖：
  - 房间信息返回自定义队长码。
  - 队长通过队伍码加入并绑定对应队伍。
  - 观众通过观众码加入。
  - 队长使用 sessionToken 提交 command 成功。
  - 观众使用 sessionToken 写入被拒绝。
  - 无效 sessionToken 被拒绝。
- 本轮仍为内存临时身份方案，未接入真实账号、密码、第三方登录或数据库。
- Codex Security 差异范围审查结论：写入 command 入口已从客户端自报 `roleBinding` 切换为服务端 sessionToken 解析；回归覆盖观众越权和无效 session，未发现本轮新增可利用越权路径。

## 2026-05-26：M2 多人端服务端骨架

### 已完成

- 新增 `apps/server/memory-store.js`，提供内存赛事状态、事件发布和 SSE 订阅管理。
- 新增 `apps/server/server.js`，使用 Node 原生 HTTP 实现无外部依赖服务端骨架。
- 新增 `npm run start:server`，默认启动 `http://127.0.0.1:4196/health`。
- 已实现接口：
  - `GET /health`：健康检查和规则版本。
  - `POST /api/tournaments`：创建赛事并写入 `TournamentCreated` 事件。
  - `GET /api/tournaments/:id/snapshot`：读取公开快照。
  - `GET /api/tournaments/:id/events`：SSE 事件流，连接后先推送当前快照。
  - `POST /api/tournaments/:id/commands`：提交 command，经角色绑定、权限、版本和幂等预检后追加事件。
- 当前存储为内存 store；数据库尚未接入，后续可按相同 store 边界替换 PostgreSQL。

### 验证

- 回归测试新增 M2 覆盖：
  - 健康检查返回服务名和规则版本。
  - 创建赛事推进 `stateVersion` 并写入创建事件。
  - 快照接口返回赛事名和公开状态。
  - SSE 连接先推送 snapshot。
  - 队长购买 command 通过 API 追加事件。
  - 重复 command 通过 API 幂等返回同一事件。
  - 观众角色通过 API 写入被拒绝。
- 本轮新增本地 HTTP API 和 SSE 入口，继续使用内存 store；未接入数据库、登录态、外部依赖或持久化凭据。
- Codex Security 差异范围审查结论：API 写入入口会调用 `packages/shared` 和 `packages/rules` 做角色、权限、队伍边界、版本和幂等预检；当前发现的越权风险已有伪造角色、观众写入、队长跨队和重复 command 回归覆盖。

## 2026-05-26：M1 多人端规则核心骨架

### 已完成

- 新增 `packages/shared`，集中维护多人端规则版本、状态 schema 版本、角色、Command 类型、Event 类型、角色可执行命令和基础协议校验。
- 新增 `packages/rules`，建立服务端权威状态的最小运行时骨架，包含：
  - `createAuthorityState()` 创建权威状态。
  - `preflightCommand()` 校验规则版本、状态版本、角色绑定、队长队伍边界、暂停状态和幂等记录。
  - `appendEvent()` 追加权威事件并推进 `stateVersion`。
  - `acceptCommandAsEvent()` 将通过预检的 command 转成事件，重复 command 返回首次事件。
- 新增 `packages/README.md`，说明当前共享包边界和后续迁移方向。
- 回归测试新增 M1 覆盖：
  - 队长购买 command 可生成权威事件并推进版本。
  - 重复 command 幂等返回同一个事件。
  - 过期 `baseVersion` 被拒绝。
  - 队长跨队操作被拒绝。
  - 客户端伪造裁判角色会被服务端角色绑定拒绝。
  - 观众角色不能创建写入型 command。

### 验证

- 验证命令按任务钩子执行记录为准。
- 本轮只新增本地共享规则模块和回归测试，不引入网络监听、数据库、外部依赖或用户输入入口。
- Codex Security 差异范围审查结论：已将服务端角色绑定作为 `acceptCommandAsEvent()` 必填参数，拒绝信任客户端自报角色；回归覆盖伪造裁判角色、观众写入、队长跨队、过期版本和重复 command，未发现本轮新增可利用越权路径。

## 2026-05-26：多人端隔离副本基线

### 已完成

- 按“多人端另起一个副本，在副本上改动”的口径，创建 `apps/multiplayer/`。
- 将 `E:\only_why\HEXCORE2.0\multiplayer` 从普通目录调整为 Git worktree，直接跟踪 `codex/multiplayer-realtime`；原普通目录已备份为 `E:\only_why\HEXCORE2.0\multiplayer_普通副本备份_20260526_173418`。
- 确认没有覆盖同级 `E:\only_why\HEXCORE2.0\hex-core2.0` 裁判端仓库；当前 worktree 列表中 `hex-core2.0` 保持裁判端，`multiplayer` 维护多人端。
- 将当前裁判端 `index.html`、`src/` 和 `public/assets/` 复制到多人端副本中，副本资产路径为 `apps/multiplayer/assets/`。
- 新增 `scripts/serve-multiplayer.js`，多人端默认使用 `http://127.0.0.1:4186/`，不复用根目录裁判端 `4176` 端口。
- `scripts/serve-multiplayer.js` 默认服务当前 worktree 内的 `apps/multiplayer/`；如需调试其它目录，可临时使用 `MULTIPLAYER_APP_ROOT` 覆盖。
- 移除过渡期的 `scripts/sync-multiplayer-copy.js` 和 `npm run sync:multiplayer`，多人端开发完成后直接在 `E:\only_why\HEXCORE2.0\multiplayer` 提交并推送。
- 新增 `npm run start:multiplayer`，后续多人端可独立启动。
- 新增 `apps/multiplayer/README.md`，明确多人端优先在副本中演进，根目录裁判端继续保持客户发布版稳定。
- 回归测试新增多人端副本隔离检查，覆盖副本首页、副本源码、副本资产、服务路径穿越和根目录脚本/文档不可暴露。

### 验证

- 验证命令按任务钩子执行记录为准。
- 本轮新增一个本地静态服务入口和副本目录，继续使用路径白名单和根目录限制；未引入数据库、登录鉴权或外部依赖。
- Codex Security 差异范围审查结论：`scripts/serve-multiplayer.js` 只允许访问副本内 `index.html`、`src/` 和 `assets/`，使用 `path.resolve` + `path.relative` 限制路径必须留在 `apps/multiplayer/` 内；回归已覆盖根目录脚本/文档不可访问、目录穿越和非法 URL 编码，未发现新增可利用路径穿越或目录暴露问题。

## 2026-05-26：多人实时系统开发计划完善

### 已完成

- 将 `docs/06_开发计划.md` 的“未来多人扩展”扩展为“多人实时系统开发计划”。
- 明确多人端在 `codex/multiplayer-realtime` 分支维护，`main` 继续服务裁判端客户发布版。
- 补充多人端产品边界、角色权限矩阵、目标架构、实时同步与并发方案、数据模型、旧版本导入策略、分阶段实施路线、关键业务流程、技术选型建议、验收门禁和开发约束。
- 保留重复海克斯冲突裁决储备，并把裁决原则纳入多人端服务端权威和实时校验口径。

### 验证

- 本轮属于方案和开发计划文档整理，不改渲染页面、运行逻辑、依赖、鉴权、导入导出实现或服务端入口。
- 验证命令按任务钩子执行记录为准。
- 安全审查结论：本轮不新增信任边界，无需追加全量安全扫描。

## 2026-05-26：班德尔保卫战 Day 布局优化

### 已完成

- 将班德尔保卫战赛程页的 Day 1 / Day 2 从左右并排改为上下单列分布，解决比赛卡在半宽面板中显示不全的问题。
- 每个 Day 内部继续保留 5 列对阵矩阵，保持每天 25 场的扫读结构。
- 将项目版本提升为 `2.0.4`，同步 `package.json`、运行界面版本元信息、回归断言和 `CHANGELOG.md`。
- `docs/06_开发计划.md` 已补充班德尔赛程布局验收项。

### 验证

- `npm test` 通过，输出 `regression ok: 49 tests`。
- `git diff --check` 通过，仅有仓库 CRLF 换行提示。
- 内置 Browser 打开 `http://127.0.0.1:4176/`，进入赛程页确认 Day 1 / Day 2 为上下单列分布；两个 Day 面板宽度均为 1540px，控制台 warning/error 数量为 0。

## 2026-05-26：S7 班德尔保卫战赛制

### 已完成

- 在赛程页新增“生成班德尔保卫战”，支持两天 5x5 全交叉阵营积分赛。
- 将项目版本提升为 `2.0.3`，同步 `package.json`、运行界面版本元信息、回归断言和 `CHANGELOG.md`。
- 本地队伍作为班德尔阵营，外地队伍作为入侵者阵营；每天 25 场，两天共 50 场。
- 每场可录入双方比分和约德尔登场人数，系统自动计算班德尔胜场分、入侵者胜场分和约德尔登场加分。
- 50 场完成后自动判断：分差大于 5 直接产生获胜阵营；分差不超过 5 开启“最强约德尔人 vs 最强侵略者”BO5，胜方额外 +10 分。
- 赛程页新增阵营积分榜、队伍贡献榜、Day 1 / Day 2 矩阵和阵营胜利展示。
- `docs/06_开发计划.md` 已写入本次赛制规则和验收口径。

### 验证

- `npm test` 通过，输出 `regression ok: 48 tests`。
- `git diff --check` 通过，仅有仓库 CRLF 换行提示。
- 内置 Browser 打开 `http://127.0.0.1:4176/`，进入赛程页生成班德尔保卫战后，DOM 显示 2 个 Day 面板、50 张比赛卡、阵营积分和约德尔登场字段，控制台 warning/error 数量为 0。
- Browser 截图接口在当前标签页返回超时；页面身份、非空页面、错误覆盖层、console 和交互结果均已通过 DOM 证据确认。
- 本轮属于本地赛程 UI、状态和计分逻辑，不新增外部输入、网络、文件写入、命令执行或鉴权边界；无需追加全量安全审查。

## 2026-05-26：维护版 2.0.2 与旧资料清理

### 已完成

- 按 GitHub 上传规则将项目版本提升为 `2.0.2`，同步 `package.json`、运行界面版本元信息和回归断言。
- 新增 `CHANGELOG.md` 作为固定版本更新记录，后续版本变化和 GitHub 推送前必须同步维护。
- 统一项目维护口径：当前只维护 GitHub，推送前必须更新版本号，不再推送 Gitee。
- 删除过期的无金币规则、四轮金币抽卡迁移方案、历史 UI 方案、旧官方规则 DOCX 和 `legacy-prototype/` 旧原型目录。
- 修正海克斯攻略 DOCX 中 `背水一战` 的旧触发条件，明确该海克斯不检查本轮购买权，满员且候选足够即可发动。

### 验证

- `npm test` 通过，输出 `regression ok: 46 tests`。
- `git diff --check` 通过，仅有 Git 行尾转换提示。
- 旧归档、旧文档路径、旧 Gitee 上传口径和旧背水条件关键字扫描无残留。

## 2026-05-25：客户发布版 2.0.1 与版本管理

### 已完成

- 将项目代码版本提升为 `2.0.1`，`package.json` 与运行界面统一展示 `HEXCORE 2.0 v2.0.1 裁判端`。
- 侧边栏流程信息和系统设置页改为读取统一版本元信息，避免发布包版本与界面显示不一致。
- 回归测试新增版本展示断言，确保后续版本升级不会只改包名漏改界面。
- 重新打包客户发布版时，`更新说明_客户版.txt` 按倒序追加新版本说明，新版本内容置于文件最上方。

### 验证

- `npm test` 通过，输出 `regression ok: 46 tests`。
- `git diff --check` 通过，仅有仓库 CRLF 换行提示。
- 通过本地 HTTP 服务读取新版前端资源，确认 `src/core/sample-data.js` 包含 `2.0.1` 版本元信息，`src/ui/referee-console.js` 使用统一版本展示函数。
- 已生成客户发布 zip：`output/release/HEXCORE2.0_客户发布版_v2.0.1_20260525_190231.zip`。
- 已校验压缩包根目录包含 `更新说明_客户版.txt` 与 `使用说明_先读我.txt`，且更新说明开头为 `v2.0.1` 版本内容。

## 2026-05-25：决赛冠军展示

### 已完成

- 赛程页新增冠军展示区：决赛保存比分并产生冠军后，在赛程控制区下方显示独立的金色强调展示。
- 展示内容包含冠军队伍、阵营、队长、决赛比分、阵容人数、剩余金币、持有海克斯数量和亚军队伍。
- 回归测试补齐从首轮到决赛完成的赛程链路，断言冠军展示区、冠军名、亚军队伍和决赛信息均存在。
- 同步更新 `docs/06_开发计划.md`，记录冠军展示验收项。

### 验证

- `npm test` 通过，输出 `regression ok: 46 tests`。
- `git diff --check` 通过，仅有 CRLF 换行提示。
- 内置 Browser 打开 `http://127.0.0.1:4176/` 验证页面非空、赛程页可正常渲染、控制台无 warning/error；当前浏览器本地状态未完成决赛，冠军最终态由回归测试覆盖。

### 安全审查结论

- 本轮修改为本地赛程完成态 UI 展示和回归断言，不新增外部输入、网络、文件写入、命令执行或鉴权边界；无需追加全量安全审查。

## 2026-05-25：赛程页布局压缩与槽位队名显示

### 已完成

- 赛程页顶部控制区调整为两行展示：统计指标一行，阵营对抗开关、说明和赛程操作按钮一行，降低面板高度。
- 缩短阵营对抗说明文案，保留“左蓝本地 vs 右红外地；取消后全随机”的关键信息。
- 赛程首轮槽位内队伍名改为优先完整显示，`更换` 与 `移出` 分行排列，避免窄槽位把队伍名截断。
- 同步更新 `docs/06_开发计划.md`，记录本次已确认的 UI 调整。

### 验证

- `npm test` 通过，输出 `regression ok: 46 tests`。
- `git diff --check` 通过，仅有 CRLF 换行提示。
- 内置 Browser 打开 `http://127.0.0.1:4176/` 验证：赛程控制面板约 151px 高，统计与控制分两行显示；`海斗7队` 在赛程槽位内完整显示；控制台无 warning/error。

### 安全审查结论

- 本轮修改为本地 UI 样式、展示文案和回归断言同步，不新增外部输入、网络、文件写入、命令执行或鉴权边界；无需追加全量安全审查。

## 2026-05-25：开发手册完成态与全量文档同步

### 已完成

- 将 `docs/06_开发计划.md` 调整为完成态开发手册，统一当前回归数量、验证入口和剩余风险说明。
- 同步 `docs/01_架构设计.md`、`docs/03_裁判代执行流程.md`、`docs/04_轮抽与顺位引擎设计.md`、`docs/05_海克斯引擎设计.md`、`docs/07_技术评估.md`、`docs/14_阵营锁定10队金币商店模式_实施规范.md` 的现行金币商店口径。
- 移除过期的无金币、四轮迁移和历史设计方案资料，避免旧规则继续被误认为当前规范。
- 同步更新最新海克斯介绍指南、攻略文档和实施规范 DOCX，并按当前主题、弹窗、顺位、血雾、神兵天降、背水一战等最新规则保持一致。
- 修正 `scripts/regression.js` 中 `雪定饿的喵 + 加急调货` 用例的随机前提：先选择存在同阵营同费用替换目标的商店卡位，避免回归测试因随机商店第 0 张不可替换而偶发失败。

### 验证

- `npm test` 连续通过，输出 `regression ok: 43 tests`。
- `git diff --check` 通过。
- `npm run post-task -- --status=complete --doc=docs/06_开发计划.md` 通过。
- DOCX 使用 Word 导出 PDF 后渲染为 PNG 页面进行可视化复查。

### 安全审查结论

- 本轮修改文档、DOCX 说明和一处本地回归测试前提，不新增外部输入、网络、持久化、命令执行或运行时代码入口；无需追加全量安全审查。

## 2026-05-25：基础项目文档同步当前金币商店版本

### 已完成

- 更新 `docs/00_项目总览.md`，将项目定位从旧版无金币轮抽修正为当前“阵营锁定 10 队金币商店模式”。
- 更新 `docs/02_数据模型.md`，补齐当前 `gold_shop` 设置、双阵营、1-5 费、队长锁定、金币商店、海克斯、赛程和 UI 状态模型。
- 更新 `docs/08_UI设计规范.md`，同步实时抽选、直接点击购买、队伍阵容看板、海克斯详情弹窗、PNG 图标、选手导入和赛程页面规范。

### 验证

- 内置浏览器当前已打开 `http://127.0.0.1:4176/`，页面标题为 `HEXCORE 2.0 裁判控制台`。
- 本轮为文档同步，不改变运行时代码和页面行为。

### 安全审查结论

- 本轮只修改 Markdown 文档，不新增外部输入、文件写入、命令执行、网络访问或持久化入口。

## 2026-05-24：赛程重排与手动轮空

### 已完成

- `docs/06_开发计划.md` 新增赛程重排完善计划，并补齐验收标准。
- 阵营对抗首轮单边拖入队伍后进入 `待补齐`，不再自动显示轮空或生成后续晋级。
- 新增“确认轮空”：只有裁判确认后，单边队伍才轮空晋级。
- 新增“移出”：首轮普通槽位和已确认轮空卡都可将队伍移出赛程，队伍回到拖拽队伍池。
- 新增“清空本场”：清空首轮单场 A/B 队伍、比分、胜者和轮空确认。
- 移出、清空、重新拖入、保存比分、确认轮空后统一重算后续晋级链。

### 验证

- `npm test` 通过，输出 `regression ok: 39 tests`。
- Edge headless 验证赛程页面：
  - 单边拖入后显示“待补齐 / 确认轮空 / 移出”，不显示“轮空晋级”。
  - 确认轮空后显示“轮空晋级”。
  - 移出后场次回到空槽，队伍池显示该队伍“待放入”。

### 安全审查

- 本次改动为本地裁判端赛程状态机、UI 和回归测试更新，不涉及新增外部输入解析、文件导入导出、命令执行、鉴权或网络信任边界。
- 未触发全量安全审查；后续若赛程导入/导出或远端同步接入，再按计划执行 Codex Security 审查。

## 2026-05-23：post-task 循环重试上限

### 已完成

- `post-task` 增加连续 `incomplete` 上限：
  - 默认同一任务文档最多连续 5 次。
  - 支持 `--max-attempts=<次数>` 或 `POST_TASK_MAX_ATTEMPTS` 临时调整。
  - 超过上限后脚本失败退出，要求停止自动重试并等待用户确认。
- 循环状态按任务文档记录，默认写入系统临时目录。
- `complete` 通过后会清零对应任务文档的连续 `incomplete` 计数。
- `--state-file` 只允许写入项目目录或系统临时目录，避免任意路径写入。
- `AGENTS.md` 与 `docs/12_任务后置钩子.md` 已同步上限规则。

### 验证

- 新增回归用例覆盖连续 `incomplete` 超过上限会失败退出。
- 已执行红绿验证：新增用例先失败，补齐实现后 `npm test` 通过，输出 `regression ok: 17 tests`。

## 2026-05-22：任务后置钩子

### 已完成

- 新增项目级 `AGENTS.md`，固定任务完成后的协作规则：
  - 本地门禁通过后判断计划状态。
  - 计划完成时进入 Codex Security 全量审查、修复、验证和按需推送流程。
  - 计划未完成时继续按开发计划推进，UI/交互任务使用 Build Web Apps 流程，较大改动后使用 Codex Security 审查和修复。
- 新增 `docs/12_任务后置钩子.md`，记录后置流程、完成判定、推送策略和 Mermaid 流程图。
- 新增 `scripts/post-task-hook.js`：
  - 支持 `--status=complete` 与 `--status=incomplete`。
  - 检查必要文档、回归脚本和 `package.json` 测试入口。
  - 输出当前 Git 工作树和下一步分支。
- `package.json` 新增脚本：
  - `npm run post-task -- --status=incomplete`
  - `npm run post-task -- --status=complete`

### 验证

- `node --check scripts\post-task-hook.js` 通过。
- `npm test` 通过，输出 `regression ok: 16 tests`。
- `npm run post-task -- --status=incomplete` 通过，并正确输出“继续执行开发计划”分支。
- `git diff --check` 通过，仅有 CRLF 换行提示。

### 安全审查

- 新增脚本仅接收白名单状态值：`complete` 或 `incomplete`。
- 脚本调用 `git status --short` 使用 `execFileSync` 参数数组，不经过 shell 拼接，未发现命令注入路径。
- 脚本只读取项目内固定文件，不接受外部路径输入，未发现路径穿越风险。

## 2026-05-22：流程阶段机与前置检查清单

### 已完成

- 新增流程阶段 selector：
  - 数据准备。
  - 队长确认。
  - 队长抽海克斯。
  - 队员抽选。
  - 阵容确认。
- 新增统一前置检查清单：
  - 队伍数量。
  - 队伍名称。
  - 队长确认。
  - 海克斯抽取。
  - 阵容一致性。
  - 可选人数。
  - 卡池容量。
- 实时抽选页在前置流程未完成时显示当前阶段、检查项状态和修复入口。
- 点击检查项可直接跳转到对应页面，例如队长确认跳转队伍管理、可选人数跳转选手库。
- 抽选就绪条件收紧：必须完成队长选手指定、海克斯抽满、阵容一致性和可选人数检查后，才进入队员抽选阶段。
- 回归脚本增加流程阶段机测试，并将测试就绪状态显式补齐队长和可选选手，避免测试绕过产品规则。

### 验证

- `npm test` 通过，输出 `regression ok: 17 tests`。
- 内置浏览器验证 `http://127.0.0.1:4176/`：
  - 页面标题为 `HEXCORE 2.0 裁判控制台`。
  - 实时抽选页显示 7 项流程检查。
  - 当前阶段可显示为 `队长确认`。
  - 点击 `队长确认` 检查项可跳转到队伍管理页。
  - 控制台无 error/warn。

### 安全审查

- 威胁模型：当前改动影响本地裁判端流程门禁和 HTML 渲染，主要风险为导入状态/本地状态中的文本进入 `innerHTML` 或脚本钩子被命令注入。
- 发现阶段：
  - 流程检查项的 `label/detail/status/view` 来自 selector 内部固定结构或已归一化状态。
  - 渲染前已对文本字段使用 `escapeHtml`。
  - 跳转动作使用 `safeJsonString(item.view)`，且 `view` 为固定内部值。
  - `post-task-hook.js` 对 `status` 使用 `complete/incomplete` 白名单，`git status` 使用 `execFileSync` 参数数组，不经过 shell 拼接。
- 结论：未发现需要修复的可利用安全问题。

## 2026-05-19：阶段 1 / 阶段 2 最小闭环

### 已完成

- 保持 `index.html` 可直接通过 `file://` 打开。
- 将原单文件 UI 拆分为普通脚本模块，避免 ES Module 本地加载问题。
- 新增状态层：
  - `src/core/sample-data.js`
  - `src/core/app-state.js`
  - `src/core/event-store.js`
- 新增引擎层：
  - `src/engines/turn-order-engine.js`
  - `src/engines/pool-engine.js`
  - `src/engines/probability-engine.js`
  - `src/engines/assignment-engine.js`
  - `src/engines/hexcore-engine.js`
- 新增 UI 层：
  - `src/ui/icons.js`
  - `src/ui/referee-console.js`
- `src/main.js` 调整为启动和动作绑定入口。
- 实现最小裁判操作闭环：
  - 抽卡。
  - 选择卡牌。
  - 选手入队。
  - 下一位。
  - 跳过。
  - 暂停。
  - 撤销提示。
  - 使用启元、致盲吹箭、双发快射。
- 致盲效果已有独立视觉状态。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 通过 Node 模拟 `window/document` 做运行时冒烟测试：
  - 页面首次渲染成功。
  - 致盲海克斯可触发。
  - 下一位、抽卡、选卡、事件日志更新可执行。

### 暂未完成

- 未接入真实 Excel 导入。
- 未实现完整 22 个海克斯。
- 未实现本地持久化。
- 未实现真实回退。
- 未实现浏览器截图自动化验证；当前环境缺少 Playwright 浏览器可执行文件。

## 2026-05-19：裁判工作台可用性增强

### 已完成

- 新增本地持久化服务：
  - `src/services/storage-service.js`
  - 使用 `localStorage` 保存当前裁判端状态。
  - 页面刷新时优先恢复当前状态和当前抽卡结果。
- 新增导出服务：
  - `src/services/export-service.js`
  - 支持导出事件日志。
  - 支持导出完整状态备份。
- UI 增强：
  - 侧栏赛事信息增加“导出状态备份”按钮。
  - 右侧事件日志“导出日志”按钮接入真实导出。
  - 已选择卡牌后按钮显示“已选择”，并在逻辑层阻止重复入队。
- 启动逻辑优化：
  - 若已有 `currentDraw`，刷新页面后直接恢复，不再强制重新抽卡。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- 运行时冒烟测试通过：
  - 首次渲染。
  - 本地状态保存。
  - 致盲海克斯。
  - 下一位。
  - 抽卡。
  - 选卡。
  - 重复选卡保护。
  - 导出事件日志。
  - 导出状态备份。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。

## 2026-05-19：撤销、导入与本地重置

### 已完成

- 新增历史快照服务：
  - `src/services/history-service.js`
  - 支持裁判操作前保存状态快照。
  - 支持真实撤销上一步，恢复队伍、选手、海克斯、轮抽和事件状态。
- 导入状态备份：
  - 侧栏新增“导入状态备份”。
  - 读取 JSON 后校验基础结构。
  - 导入前自动保存快照，便于撤销。
- 重置本地状态：
  - 侧栏新增“重置本地状态”。
  - 清除本地存储并刷新页面，恢复示例初始数据。
- UI 反馈：
  - 侧栏显示当前可撤销步骤数。
  - 撤销按钮显示可撤销数量。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- 运行时冒烟测试通过：
  - 选卡入队。
  - 撤销恢复队伍状态。
  - 事件日志导出。
  - 状态备份导出。
  - 状态备份导入。
  - 本地状态重置。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。

## 2026-05-19：轮抽推进、完成态与事件筛选

### 已完成

- 顺位引擎增强：
  - 只让未满员队伍进入有效顺位。
  - 支持从当前队长推进到下一队长、下一轮或选秀完成。
  - 到达最大轮次或所有队伍满员后进入 `completed` 完成态。
- 状态兼容增强：
  - 旧本地缓存或旧状态备份缺少 `maxRounds`、`ui`、`undoStack` 等字段时自动补齐。
  - 导入旧状态备份时默认恢复到“全部事件”筛选，避免日志被筛选隐藏。
- UI 增强：
  - 顶栏显示选秀完成状态。
  - 顺位面板显示当前有效队伍数。
  - 事件日志筛选接入真实状态，支持全部事件、海克斯、选手入队、警告。
  - 无当前队长、空筛选结果、完成态抽卡等边界均显示明确反馈。
- 裁判操作增强：
  - 抽卡时识别选秀完成、无可操作队长、队伍满员和卡池不足。
  - 下一位操作会记录“进入下一位”“回合推进”或“选秀完成”事件。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时冒烟测试通过：
  - 页面首次渲染。
  - 启动抽卡。
  - 选卡入队。
  - 事件筛选。
  - 切换下一位。
  - 队伍满员后进入完成态。
  - 完成态阻止继续抽卡。
  - 旧状态备份导入后字段归一化。
- 浏览器插件验证：
  - `file://` 访问被浏览器安全策略阻止。
  - 改用本地静态服务后页面可打开，但截图和交互命令出现插件底层超时，本轮未取得稳定截图证据。

## 2026-05-19：低风险海克斯自动化基础

### 已完成

- 扩展示例海克斯数据：
  - 巨人杀手。
  - 舞会女王。
  - 恶魔契约。
  - 稳扎稳打。
- 卡池引擎增强：
  - 舞会女王自动反转持有者个人卡池顺序。
  - 巨人杀手自动在第1轮进入猛犸池、第4轮进入侏儒马池。
- 顺位引擎增强：
  - 恶魔契约第1-3轮自动移动到第1顺位。
  - 恶魔契约第4轮自动移动到最后顺位。
  - 启元按优先级高于恶魔契约执行。
  - 顺位解释中写入自动规则原因。
- 自动分配能力：
  - Assignment Engine 新增按卡池随机分配。
  - 稳扎稳打由裁判触发后自动从当前有效池随机入队，并推进到下一位。
- UI 增强：
  - 被动海克斯显示为“被动规则：自动生效”。
  - 被动海克斯按钮禁用，避免裁判误触。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时冒烟测试通过：
  - 舞会女王第2轮进入上等马池。
  - 巨人杀手第1轮进入猛犸池、第4轮进入侏儒马池。
  - 恶魔契约第2轮移动到第1顺位，并产生顺位解释。
  - 稳扎稳打自动分配1名选手并推进到下一位。
  - 启元使用后优先级高于恶魔契约。
- 浏览器插件验证：
  - 本地静态服务 `http://127.0.0.1:4173/index.html` 可加载页面。
  - DOM 首屏包含裁判控制台、抽卡区、海克斯区，控制台无错误日志。
  - 插件点击和截图命令仍出现 CDP 超时，未取得稳定截图和真实点击证据。

## 2026-05-19：开饭啦与卡池解释

### 已完成

- 新增顺位类/自选类海克斯：
  - `开饭啦`：裁判触发后，系统展开当前有效池全部可用选手。
  - 裁判可从全池自选列表中选择1名选手入队。
- 抽卡引擎增强：
  - 新增 `drawAll`，用于生成全池自选抽卡结果。
  - 抽卡结果支持 `pickMode: open_pick` 和执行原因。
- UI 增强：
  - 抽卡区支持“全池自选”状态。
  - 全池自选状态使用更密的卡牌网格，适配超过3张选手卡。
  - 规则说明新增“卡池原因”，显示基础卡池和海克斯造成的卡池变化。
- 卡池引擎增强：
  - 新增 `explain`，输出当前队长的基础卡池、有效卡池和变更原因。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时冒烟测试通过：
  - 开饭啦进入全池自选模式。
  - 展示当前有效池全部可用选手。
  - UI 显示“全池自选”和自选网格。
  - 规则面板显示卡池解释。
  - 选中后正常入队。
- 浏览器插件验证：
  - 本地静态服务页面可加载。
  - DOM 包含裁判操作、卡池原因、海克斯面板。
  - 控制台无错误日志。
  - 截图命令仍出现 CDP 超时，未取得稳定截图证据。

## 2026-05-19：质变系列自动执行

### 已完成

- 新增质变系列海克斯：
  - `质变：青铜阶`：跳过当前池，直接从中等马池盲抽1人。
  - `质变：黄金阶`：跳过当前池，直接从上等马池盲抽1人。
  - `质变：棱彩阶`：跳过当前池，直接从猛犸池盲抽1人。
- Assignment Engine 增强：
  - 新增目标池盲抽自动入队能力。
- 海克斯引擎增强：
  - 质变系列触发后清空普通抽卡结果。
  - 质变系列触发后标记本轮已选。
  - 质变系列触发后自动推进到下一位队长。
  - 事件日志记录目标池和自动执行结果。
- 约束修正：
  - 内部标识避免使用 `gold`，继续保证 `src` 中无旧金币业务关键词。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时冒烟测试通过：
  - 青铜阶质变消耗中等马池1名选手并入队。
  - 黄金阶质变消耗上等马池1名选手并入队。
  - 棱彩阶质变消耗猛犸池1名选手并入队。
  - 三个质变均清空普通抽卡结果，并推进到下一位队长。
- 浏览器插件验证：
  - 本地静态服务页面可加载。
  - DOM 包含裁判操作、规则说明、海克斯面板。
  - 控制台无错误日志。
  - 截图命令仍出现 CDP 超时，未取得稳定截图证据。

## 2026-05-19：优中选优抽卡修饰

### 已完成

- 新增 `优中选优` 海克斯：
  - 作为被动规则展示。
  - 当持有者有效卡池为上等马池时自动触发。
  - 本次抽卡额外展示1张选手卡。
- 抽卡修饰能力增强：
  - Hexcore Engine 新增抽卡原因输出。
  - 普通抽卡事件会记录抽卡修饰来源。
  - 抽卡结果保存 `reason`，UI 可展示本次抽卡为何变化。
- 示例数据增强：
  - 增加上等马池可用选手，保证优中选优有足够样本展示4张。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时冒烟测试通过：
  - 第3轮 C11 持有优中选优时抽出4张卡。
  - 抽卡结果和 UI 均显示优中选优原因。
  - 事件日志记录优中选优原因。
  - 选择后只消耗最终选中的1名选手。
- 浏览器插件验证：
  - 本地静态服务页面可加载。
  - DOM 包含裁判操作、规则说明、海克斯面板。
  - 控制台无错误日志。
  - 截图命令仍出现 CDP 超时，未取得稳定截图证据。

## 2026-05-19：摄影艺术家全局卡池互换

### 已完成

- 新增 `摄影艺术家` 海克斯：
  - 第1-3轮可手动触发。
  - 本轮基础卡池与下一轮基础卡池互换。
  - 影响所有队长，而不是只影响持有者。
- 卡池引擎增强：
  - 新增全局卡池互换运行时效果。
  - 有效卡池计算先应用全局互换，再应用个人卡池类海克斯。
  - 卡池解释会写入摄影艺术家造成的全局卡池变化。
- 海克斯引擎增强：
  - 触发后清空当前抽卡结果，要求裁判重新按新卡池抽卡。
  - 事件日志记录本轮与下轮卡池互换结果。
  - 第4轮误触不会消耗使用次数。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时冒烟测试通过：
  - 第2轮使用摄影艺术家后，当前轮从中等马池切换为上等马池。
  - 效果影响其他队长。
  - 卡池解释包含摄影艺术家原因。
  - 抽卡结果来自互换后的卡池。
  - 下一轮卡池按互换规则回到中等马池。
- 浏览器插件验证：
  - 本地静态服务页面可加载。
  - DOM 包含裁判操作、规则说明、海克斯面板。
  - 控制台无错误日志。
  - 截图命令仍出现 CDP 超时，未取得稳定截图证据。

## 2026-05-19：潘多拉魔盒固定顺位与自动分配

### 已完成

- 新增 `潘多拉魔盒` 海克斯：
  - 作为被动规则展示。
  - 持有者全程固定第3顺位。
  - 抽卡操作触发时，不生成普通抽卡结果，改为系统自动分配。
- 顺位引擎增强：
  - 支持 `fixed_position` 固定位置修饰。
  - 潘多拉固定第3顺位优先级为 700，高于恶魔契约，低于启元和裁判强制修正。
  - 顺位解释写入“潘多拉魔盒：全程固定第3顺位”。
- 自动分配增强：
  - Assignment Engine 新增评分前N随机分配能力。
  - 潘多拉从当前有效池评分前5随机分配1名选手。
  - 当前池无人时，从下一等级池继续查找可分配选手。
- 流程控制：
  - 潘多拉自动分配后清空普通抽卡结果。
  - 标记本轮已选，并自动推进到下一位队长。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时冒烟测试通过：
  - C5 固定在第3顺位。
  - 顺位解释包含潘多拉魔盒原因。
  - 抽卡触发后自动分配1名选手。
  - 自动分配后不保留普通抽卡结果。
  - 自动推进到下一位队长。
- 浏览器插件验证：
  - 本地静态服务页面可加载。
  - DOM 包含裁判操作、规则说明、海克斯面板。
  - 控制台无错误日志。
  - 本轮截图命令成功，首屏渲染正常。

## 2026-05-19：队伍数量动态化

### 已完成

- 队伍数量不再固定为12队。
- 状态层按 `captains.length` 自动计算 `settings.totalTeams`。
- 新增配置边界：
  - 最少5队。
  - 最多20队。
- UI 动态显示当前队伍数量和允许范围。
- 顺位条根据当前有效队伍数动态生成列数。
- 底部阵容概览根据实际队伍数动态生成列数。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时冒烟测试通过：
  - 默认12队渲染。
  - 5队状态导入后渲染、顺位和底部阵容概览均为5队。
  - 20队状态导入后渲染、顺位和底部阵容概览均为20队。
- 浏览器插件验证：
  - 本地静态服务页面可加载。
  - DOM 包含裁判操作和默认12队阵容概览。
  - 控制台无错误日志。
  - 当前窄侧栏布局会隐藏赛事信息区，因此队伍范围文本不出现在可见 DOM 快照中。

## 2026-05-19：5-20队深度回归与安全修复

### 已完成

- 深度遍历 5-20 队不同队伍数量。
- 导入状态备份增加安全校验：
  - 队伍数量必须在5-20之间。
  - 队长必须有有效ID。
  - 队长ID不能重复。
- UI 动态文本增加 HTML 转义：
  - 队长名。
  - 选手名、游戏ID、分路、战绩字段。
  - 海克斯名和描述。
  - 规则说明。
  - 事件日志标题、时间、正文。
- 事件日志等级 class 增加白名单，非法等级回退为 `info`。

### 安全审查结论

- 威胁模型：当前版本为裁判本地单端应用，主要输入风险来自导入的状态备份 JSON。
- 发现并修复：
  - 恶意备份可导入 5-20 之外的队伍数量，造成 UI 和流程异常。
  - 恶意备份中的文本字段可能进入 `innerHTML`，存在 DOM 注入风险。
- 修复后验证：
  - 4队和21队导入会被拒绝。
  - 重复队长ID导入会被拒绝。
  - 恶意 HTML/脚本文本会以转义文本显示，不会作为 DOM 执行。

### 功能回归验证

- 遍历 5-20 队，每个队伍数均通过：
  - 状态归一化。
  - 顺位生成。
  - 动态赛制文案。
  - 动态顺位网格。
  - 动态阵容概览网格。
  - 抽卡。
  - 选卡入队。
  - 事件筛选。
  - 下一位。
  - 跳过。
  - 暂停和恢复。
  - 满员完成态。
- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 浏览器插件验证：
  - 本地静态服务页面可加载。
  - DOM 包含裁判操作和阵容概览。
  - 控制台无错误日志。
  - 本轮截图命令成功，首屏渲染正常。

## 2026-05-19：双发快射自动执行闭环

### 已完成

- 修正 `双发快射` 的自动执行语义：
  - 仅第1/2/3轮可用。
  - 使用后立即从当前有效池随机分配1名选手。
  - 同时从下一轮有效池随机分配1名选手。
  - 记录下一轮自动跳过选人效果。
  - 若在第3轮前使用，记录第3轮顺位下降1位效果。
- 卡池引擎增强：
  - 新增按指定轮次计算有效卡池能力，用于预取下一轮卡池。
- 顺位引擎增强：
  - 支持海克斯造成的整轮跳过。
  - 支持顺位下降1位修饰。
  - 自动跳过只在对应轮次首次计算时写入事件，避免重复刷日志。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时回归测试通过：
  - 第2轮使用双发快射后自动入队2名选手。
  - 正确记录第3轮跳过效果。
  - 正确记录第3轮顺位下降效果。
  - 第3轮重算顺位时该队长被自动排除。
  - 事件日志包含双发快射自动执行记录。

## 2026-05-19：背水一战自动执行闭环

### 已完成

- 新增 `背水一战` 海克斯示例配置。
- 抽卡前自动执行：
  - 第1轮和第2轮跳过抽卡与选人。
  - 系统从本轮有效池随机分配1名选手。
  - 自动推进到下一位队长。
- 第4轮规则：
  - 顺位引擎将持有者移动到第1顺位。
  - 优先级设为900，高于启元的800。
  - 抽卡时生成猛犸池全池自选列表，由裁判代执行选择。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时回归测试通过：
  - 第1轮触发背水一战后自动入队1名选手。
  - 第4轮顺位优先级高于启元并进入第1顺位。
  - 第4轮抽卡生成猛犸池全池自选列表。

## 2026-05-19：致盲吹箭目标选择与每轮限制

### 已完成

- 修正 `致盲吹箭` 的执行方式：
  - 不再默认命中下一位队长。
  - 裁判端展示可选目标队长按钮。
  - 目标列表排除当前持有者本人。
  - 本轮已被致盲过的队长不再出现在目标列表。
  - 同一持有者每轮只能使用1次。
- 运行时效果增强：
  - 致盲效果记录来源队长和目标队长。
  - 公告文案写入事件日志：“A 化身提莫，对 B 使用了致盲吹箭，哼哈哈哈哈”。
  - 被致盲队长本轮抽卡区域继续显示毛玻璃/身份隐藏状态。
- UI 增强：
  - `致盲吹箭` 海克斯行内显示目标按钮。
  - 已使用后显示“本轮已使用”。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时回归测试通过：
  - 目标列表排除自己。
  - 指定目标后目标进入本轮致盲状态。
  - 本轮已被致盲的队长不能再次被选择。
  - 同一持有者本轮第二次使用会被拒绝。
  - 被致盲队长抽卡时 UI 显示身份隐藏。

## 2026-05-19：顺位互换基础顺位规则

### 已完成

- 新增 `顺位互换` 海克斯示例配置。
- 引擎支持指定任意两名队长交换基础选人顺位。
- 互换只修改 `baseOrder`：
  - 不直接交换潘多拉魔盒、启元、恶魔契约、背水一战等海克斯产生的固定或临时顺位。
  - 重新计算顺位后，固定/临时顺位仍按优先级独立结算。
- 裁判端 UI 增强：
  - `顺位互换` 海克斯行内展示可选队长组合。
  - 队伍数量较多时组合列表在行内滚动，避免撑破面板。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时回归测试通过：
  - 两名队长基础顺位成功互换。
  - 背水一战第4轮高优先级仍优先生效。
  - 潘多拉魔盒固定顺位说明仍保留并参与结算。
  - 顺位互换全程一次，重复使用会被拒绝。
  - UI 渲染顺位互换操作入口。

## 2026-05-19：雪定饿的喵暗信息抽卡

### 已完成

- 新增 `雪定饿的喵` 海克斯示例配置。
- 抽卡引擎新增最高分/最低分二选一能力：
  - 从当前有效池取评分最高和最低的两名可用选手。
  - 系统随机决定展示身份是否互换。
  - 抽卡结果保存展示身份和真实身份。
- 裁判端选择逻辑增强：
  - UI 展示“真实身份待揭示”提示。
  - 选择后按真实身份入队。
  - 事件日志记录展示身份和真实入队身份。
- 使用限制：
  - 同一队长每轮只能使用1次。
  - 当前池不足2名可用选手时拒绝执行。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时回归测试通过：
  - 成功生成2张身份扰动卡。
  - UI 显示真实身份待揭示。
  - 选择后按真实选手入队。
  - 选择后写入暗牌揭示事件。
  - 同一队长本轮重复使用会被拒绝。

## 2026-05-19：知识来源于分解信息增强

### 已完成

- 新增 `知识来源于分解` 海克斯示例配置。
- 使用条件：
  - 当前队长必须已经拥有至少1名选手。
  - 裁判端必须选择当前队伍内已有选手作为分析目标。
  - 全程只能使用1次。
- 引擎增强：
  - 使用后写入本轮信息增强运行时效果。
  - 支持按选手评分计算全局战力顺位。
- UI 增强：
  - 海克斯行内展示可分析的已有队员。
  - 本轮抽卡卡牌显示“战力顺位 #N”。
  - 历史表现信息继续沿用现有卡牌展示。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时回归测试通过：
  - 队伍外选手不能作为分析目标。
  - 已有队员可作为分析目标。
  - 使用后本轮抽卡 UI 显示战力顺位。
  - 重复使用会被拒绝。
  - 事件日志写入信息增强记录。

## 2026-05-19：锁定契约自动绑定入队

### 已完成

- 新增 `锁定契约` 海克斯示例配置。
- 裁判端支持选择两名当前可用选手建立绑定关系。
- 引擎增强：
  - 锁定关系写入运行时效果。
  - 任意一个绑定选手通过正常选卡、自动分配或海克斯分配入队时，另一名可用绑定选手自动加入同一队伍。
  - 自动入队不递归触发新的锁定契约，避免循环。
  - 若另一名绑定选手已不可用，契约自动失效并写入事件日志。
- UI 增强：
  - `锁定契约` 行内展示可绑定选手组合。
  - 组合列表使用滚动区域，避免撑破面板。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时回归测试通过：
  - 可成功建立两名可用选手的锁定契约。
  - 选择其中一名后，另一名自动入队。
  - 队伍人数增加2名。
  - 写入锁定契约触发事件。
  - 全程一次，重复使用会被拒绝。

## 2026-05-19：神秘贤者·盲盒转队与补偿回合

### 已完成

- 新增 `神秘贤者·盲盒` 海克斯示例配置。
- 抽卡引擎增强：
  - 支持从本轮卡池所有选手中抽卡。
  - 抽卡范围包含已被选中的选手。
- 入队引擎增强：
  - 支持将已入队选手从原队伍转入当前队伍。
  - 转队时同步更新原队伍、目标队伍和选手 `teamId`。
  - 状态归一化会根据队长阵容补齐旧数据中缺失的 `teamId`。
- 顺位补偿：
  - 盲盒选中其他队长的选手后，原队长获得1次补偿抽卡和选人机会。
  - 补偿回合插入在当前队长之后。
- UI 增强：
  - 盲盒抽卡会标识已在其他队伍的选手。
  - 已选选手缺少英雄字段时使用兜底展示，避免渲染失败。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时回归测试通过：
  - 旧已选选手会在状态归一化时补齐 `teamId`。
  - 盲盒选中他队选手后，该选手脱离原队伍并加入当前队伍。
  - 原队长获得当前顺位之后的补偿回合。
  - 写入转队事件和补偿回合事件。

## 2026-05-19：地狱三头犬连续三池自选

### 已完成

- 新增 `地狱三头犬` 海克斯示例配置。
- 第1轮专用规则：
  - 触发后依次进入侏儒马池、中等马池、上等马池三段自选。
  - 每段抽卡结果保存对应限时：15秒、10秒、5秒。
  - 每段选卡入队后自动推进到下一段。
  - 三段完成或队伍满员后标记本轮已选。
- 裁判端增强：
  - 抽卡标题显示 `地狱三头犬连选`。
  - 提示区显示当前段限时。
  - 操作区新增 `超时随机`，当前段超时后可由系统随机选择一张入队。
- 示例数据增强：
  - 新增可用侏儒马选手，保证第1段有可选候选。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时回归测试通过：
  - 第1轮可成功触发地狱三头犬。
  - 第一段进入侏儒马池，限时15秒。
  - 超时随机可完成当前段入队。
  - 后续自动进入中等马池和上等马池。
  - 三段完成后队伍增加3名选手，并写入完成事件。

## 2026-05-19：潘多拉魔盒冲突禁用

### 已完成

- 引擎增加潘多拉冲突处理：
  - 潘多拉持有者不能再使用启元、开饭啦、盲盒、雪定饿的喵、地狱三头犬、双发快射、稳扎稳打和质变系列等自主选人或抽卡类效果。
  - 被禁用海克斯触发时直接拒绝执行，并写入事件日志。
- UI 增强：
  - 被潘多拉禁用的海克斯显示 `潘多拉魔盒：该效果失效`。
  - 操作按钮显示 `失效` 并禁用。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时回归测试通过：
  - 潘多拉持有者配置开饭啦时，开饭啦被识别为禁用。
  - 禁用后不会生成全池自选抽卡。
  - 事件日志写入潘多拉禁用原因。
  - UI 显示失效状态。

## 2026-05-19：启元同轮优先队列

### 已完成

- 修正 `启元` 顺位语义：
  - 同轮第1个使用者固定第1顺位。
  - 同轮第2个使用者固定第2顺位。
  - 第N个使用者固定第N顺位。
- 顺位引擎增强：
  - 支持运行时 `fixed_position` 修饰。
  - 启元优先级保持800，高于恶魔契约，低于背水一战第4轮。
- 顺位解释增强：
  - 启元解释写入“本轮第 N 个使用者，固定第 N 顺位”。

### 验证

- 所有 `src/**/*.js` 通过 `node --check`。
- `src` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 运行时回归测试通过：
  - 同轮两个启元使用者按使用顺序进入第1、第2顺位。
  - 顺位解释包含各自队列位置。

## 2026-05-19：统一海克斯回归脚本与部署访问

### 已完成

- 新增 `scripts/regression.js`。
- 通过 Node VM 加载静态页面脚本，不依赖浏览器即可验证核心规则。
- 覆盖 8 类关键规则和 UI 入口：
  - 启元同轮优先队列。
  - 潘多拉魔盒冲突禁用。
  - 锁定契约自动绑定入队。
  - 盲盒转队与补偿回合。
  - 地狱三头犬连续三池自选。
  - 雪定饿的喵身份互换。
  - 知识来源于分解信息加强。
  - 侧边栏视图切换与海克斯按钮 HTML 有效性。
- 在开发计划中记录统一回归入口：`node scripts\regression.js`。
- 新增 `package.json` 和 `scripts/serve.js`，默认通过 `npm start` 启动静态部署服务。
- `file://` 直接打开时显示部署访问提示，不再进入裁判控制台。
- 修复海克斯按钮的字符串引号问题，避免 `onclick` 参数被截断导致点击无响应。
- 侧边栏补齐真实视图：
  - 队伍管理。
  - 选手库。
  - 海克斯库。
  - 赛程进度。
  - 规则设置。
  - 日志导出。
  - 系统设置。
- 海克斯库新增裁判操作入口：为当前队长抽取海克斯。

### 验证

- `node scripts\regression.js` 通过，输出 `regression ok: 8 tests`。
- 所有 `src/**/*.js` 和 `scripts/**/*.js` 通过 `node --check`。
- `src` 与 `scripts` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 可视 Edge 浏览器访问 `http://127.0.0.1:4176/index.html` 验证通过：
  - 首屏显示裁判控制台。
  - 侧边栏可进入选手库。
  - 侧边栏可进入海克斯库。
  - 实时选秀页可点击 `启元` 并写入事件。
  - 海克斯库可为当前队长抽取海克斯。
- Edge 打开 `file://.../index.html` 时显示部署访问提示。

## 2026-05-19：侧边栏页面实质功能

### 已完成

- 队伍管理：
  - 新增队伍。
  - 删除队伍。
  - 重命名队长。
  - 设置当前队长。
  - 将已入队选手移回可选池。
- 规则设置：
  - 可调整队伍数量，范围保持 5-20 队。
  - 可调整每队人数。
  - 可调整当前轮次。
  - 保存后重算顺位并清空当前抽卡结果。
- 选手库：
  - 新增筛选器，支持全部、仅可选、仅已入队、指定卡池筛选。
- 海克斯库：
  - 可选择任意队长。
  - 可为指定队长抽取海克斯。
  - 可移除指定队长已持有海克斯。
- 所有关键配置变更均写入事件日志，并进入撤销快照。

### 验证

- `node scripts\regression.js` 通过，输出 `regression ok: 8 tests`。
- 所有 `src/**/*.js` 和 `scripts/**/*.js` 通过 `node --check`。
- `src` 与 `scripts` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 可视 Edge 调试 `http://127.0.0.1:4176/index.html` 验证通过：
  - 队伍管理页面可新增队伍。
  - 队伍管理页面可重命名 `c1`。
  - 队伍管理页面可切换当前队长为 `c2`。
  - 规则设置可将队伍数量保存为 14，当前轮次保存为第 3 轮。
  - 选手库筛选器可切换到仅可选。
  - 海克斯库可为 `c2` 抽取海克斯，并可移除刚抽取的海克斯。

## 2026-05-19：队伍改名与按钮反馈修复

### 已完成

- 队伍改名从浏览器弹窗改为队伍卡片内行内输入框。
- 新增 `保存名称` 按钮，直接读取输入框内容并保存。
- 改名失败、未变化、成功都会写入事件日志并显示顶部反馈提示。
- 事件日志追加时同步写入 `ui.feedback`，用于页面级操作反馈。
- 所有按钮增加统一 hover、按下、焦点反馈：
  - 鼠标移入轻微抬起和阴影。
  - 点击时下压缩放。
  - 键盘焦点显示高亮描边。
- 禁用按钮不再响应 hover/点击动效。

### 验证

- `node scripts\regression.js` 通过，输出 `regression ok: 8 tests`。
- 所有 `src/**/*.js` 和 `scripts/**/*.js` 通过 `node --check`。
- `src` 与 `scripts` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 可视 Edge 调试 `http://127.0.0.1:4176/index.html` 验证通过：
  - 在队伍管理页将 `c1` 改名为 `C1 Edge验证改名`。
  - 状态和页面文本均更新。
  - 顶部反馈提示显示改名成功。
  - 按钮 hover/active 动效 CSS 已生效。

## 2026-05-20：赛程、日志和系统设置实质操作

### 已完成

- 赛程进度：
  - 新增 4 轮赛程矩阵。
  - 每个队长每轮都有状态单元格。
  - 点击单元格可跳转到指定轮次和指定队长。
  - 跳转会清空当前抽卡结果并写入事件日志。
- 日志导出：
  - 新增导出当前筛选日志入口。
  - 新增清空日志入口。
  - 清空后保留一条 `日志清理` 反馈事件。
- 系统设置：
  - 新增系统摘要：版本、事件数量、撤销快照、最近事件。
  - 新增 `运行状态检查`。
  - 检查队伍人数上限、选手重复归属、缺失选手、归属字段不一致、基础顺位异常。
- 回归脚本覆盖新增页面入口和关键动作。

### 验证

- `node scripts\regression.js` 通过，输出 `regression ok: 8 tests`。
- 所有 `src/**/*.js` 和 `scripts/**/*.js` 通过 `node --check`。
- `src` 与 `scripts` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 无界面 Edge 调试 `http://127.0.0.1:4176/index.html` 验证通过：
  - 赛程页可显示赛程矩阵。
  - 点击第 2 轮 `c2` 单元格后，当前轮次和当前队长正确切换。
  - 日志页可显示清空日志和导出入口。
  - 清空日志后保留 `日志清理` 事件。
  - 系统设置页可显示状态摘要。
  - 运行状态检查后写入 `系统检查通过`。

## 2026-05-20：选手库和海克斯库数据准备能力

### 已完成

- 选手库：
  - 新增 `新增选手`。
  - 可编辑选手名称、位置、卡池和评分。
  - 可保存选手基础信息。
  - 可禁用未入队选手。
  - 可恢复已禁用选手。
  - 筛选器增加 `仅禁用`。
- 海克斯库：
  - 增加海克斯筛选器：全部、手动效果、被动效果、青铜/功能、黄金/干扰、棱彩/强力。
  - 每个海克斯卡片增加 `分配给该队长`。
  - 支持给指定队长直接分配指定海克斯。
  - 重复分配会提示失败，不会产生重复海克斯。
- 回归脚本覆盖新增选手、保存选手、禁用恢复、海克斯筛选和指定分配。

### 验证

- `node scripts\regression.js` 通过，输出 `regression ok: 8 tests`。
- 所有 `src/**/*.js` 和 `scripts/**/*.js` 通过 `node --check`。
- `src` 与 `scripts` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 无界面 Edge 调试 `http://127.0.0.1:4176/index.html` 验证通过：
  - 选手库可新增选手。
  - 可保存名称、位置、卡池、评分。
  - 可禁用并恢复该选手。
  - 海克斯库可切换到手动效果筛选。
  - 可给 `c2` 指定分配 `启元`。

## 2026-05-20：状态提示自动淡出

### 已完成

- 状态提示记录增加创建时间，用于判断当前提示生命周期。
- 新提示出现时会取消旧提示的清理计时，避免旧计时误删新提示。
- 状态提示显示约 2 秒后开始淡化，2.2 秒后自动从状态和页面中移除。
- 顶部提示禁用指针事件，避免遮挡下方操作。
- 回归脚本增加状态提示自动消失用例。

### 验证

- `node scripts\regression.js` 通过，输出 `regression ok: 9 tests`。
- 所有 `src/**/*.js` 和 `scripts/**/*.js` 通过 `node --check`。
- `src` 与 `scripts` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 后台浏览器验证 `http://127.0.0.1:4176/index.html` 通过：
  - 页面标题为 `HEXCORE 2.0 裁判控制台`。
  - 首屏内容正常渲染，不是空白页。
  - 点击 `刷新池子` 后立即显示状态提示。
  - 等待 2.35 秒后 `.feedback-toast` 已从页面移除。
  - 控制台无 error/warn。

## 2026-05-20：队伍管理与规则设置第一阶段增强

### 已完成

- 队伍管理：
  - 新增队伍数量配置入口，支持 5-20 队动态应用。
  - 保留新增、删除、重命名队长，并增强为完整日志和撤销快照流程。
  - 新增基础顺位上移、下移、指定位置调整。
  - 新增满员、缺员、异常队伍统计和单队状态展示。
  - 新增手动补录队员，支持从可选池补入指定队伍。
  - 手动移除队员、补录队员、顺位调整、数量调整均写入事件日志。
- 规则设置：
  - 新增最大轮数、基础抽卡张数配置。
  - 新增每轮卡池顺序配置，卡池引擎已读取该规则。
  - 新增自动随机策略和超时策略配置入口。
  - 新增海克斯启用/禁用控制，顺位、卡池、手动执行引擎均读取禁用状态。
  - 新增规则模板保存入口。
  - 保存规则前弹确认，保存后写入事件日志并重算流程。
- 回归测试覆盖：
  - 规则设置调整队伍数量、轮次、抽卡张数、每轮卡池。
  - 队伍基础顺位调整。
  - 手动补录队员。
  - 海克斯禁用。
  - 规则模板保存。

### 验证

- `node scripts\regression.js` 通过，输出 `regression ok: 9 tests`。
- 所有 `src/**/*.js` 和 `scripts/**/*.js` 通过 `node --check`。
- `src` 与 `scripts` 中没有 `gold`、`金币`、`cost`、`扣费`、`破产` 旧业务关键词。
- 后台浏览器验证 `http://127.0.0.1:4176/index.html` 通过：
  - 页面标题为 `HEXCORE 2.0 裁判控制台`。
  - 首屏内容正常渲染，不是空白页。
  - 队伍管理页显示队伍数量、状态统计、补录队员、基础顺位调整。
  - 点击 `C2` 上移后页面状态更新且无控制台错误。
  - 规则设置页显示最大轮数、基础抽卡张数、每轮卡池顺序、海克斯启用控制和模板保存。
  - 海克斯启禁入口可定位，控制台无 error/warn。

## 2026-05-22：金币商店海克斯第一批规则落地

### 已完成

- 删除废弃海克斯：
  - 定向招募
  - 顺位插队
  - 预算返还
- 修正规则：
  - 阵营封锁改为可对任意队长使用；目标本轮已行动时效果保留到下一次商店，最后一轮无后续窗口时不可选。
  - 抬价干扰改为可对任意队长使用；费用没有上限，商店卡显示醒目的 `+1` 标记，实际扣费同步增加。
- 新增第一批低风险海克斯：
  - 捐赠：获得时资金 +2。
  - 赞助回流：每局 2 次，购买实际费用不低于 3 的选手后返还 1 金币。
  - 开饭啦：第 3 轮开始时额外获得 3 金币。
  - 吸血习性：从金币最高的三名其他队长处每人获得 1 金币。
  - 巨人杀手：首次购买 4 费和 5 费卡时，各优惠 1 金币。
  - 摄影艺术家：每轮 1 次本轮专属免费刷新，不累计。
- 经济引擎调整：
  - 购买最终费用不再封顶为 5，用于支持抬价干扰无上限。
  - 刷新费用支持摄影艺术家的本轮免费刷新。
- 回归测试覆盖：
  - 废弃海克斯不会进入海克斯池。
  - 阵营封锁跨阵营目标、已行动目标延迟生效。
  - 抬价干扰跨阵营目标、费用 +1 显示和实际扣费。
  - 捐赠、赞助回流、开饭啦、吸血习性、巨人杀手、摄影艺术家基础效果。

### 验证

- `npm test` 通过，输出 `regression ok: 16 tests`。

## 2026-05-22：第一轮补 1 费刷新与规格文档校准

### 已完成

- 增加第 1 轮补 1 费刷新规则：
  - 当前商店没有 1 费卡，且同阵营可选池仍存在 1 费卡时，下一次刷新费用为 0。
  - 免费刷新可重复触发，直到刷出 1 费卡或同阵营 1 费池耗尽。
  - 同阵营 1 费池耗尽后不再触发免费刷新，可按正常规则购买 2 费及以上卡。
- UI 顶栏、商店状态栏和刷新按钮显示免费刷新原因。
- 更新阵营锁定实施规范：
  - 删除定向招募、顺位插队、预算返还等旧规则残留。
  - 补齐捐赠、赞助回流、开饭啦、吸血习性、巨人杀手、摄影艺术家和第一轮补 1 费规则说明。

### 验证

- `npm test` 通过，输出 `regression ok: 16 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- `npm run camp-loop -- --status=complete` 通过。
- 内置浏览器检查主页面无控制台 error。

## 2026-05-22：贤者的博爱金币商店版

### 已完成

- 新增 `贤者的博爱` 海克斯：
  - 每个持有者的选人阶段开始时触发。
  - 获得 `+n` 金币，`n` 为当前轮数。
  - 获得 `+1` 次可累计免费刷新次数。
- 刷新优先级：
  - 摄影艺术家免费刷新优先。
  - 第一轮缺 1 费免费刷新优先。
  - 最后才消耗贤者的博爱累计免费刷新次数。
- UI 刷新提示增加 `博爱免费`、`博爱刷新`、`消耗累计刷新次数` 等状态文案。
- 修复经济引擎内部状态写入顺序：先归一化经济状态，再写入 `wiseBenevolenceApplied`，防止同一轮刷新时重复发放奖励。

### 验证

- `npm test` 通过，输出 `regression ok: 16 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- 内置浏览器验证海克斯库显示 `贤者的博爱`，无 console error/warn。

## 2026-05-22：选手导入预览校验

### 已完成

- 选手导入改为两段式流程：
  - 读取 JSON/CSV 后先生成导入预览。
  - 裁判确认后才写入选手库。
  - 取消预览不会改动选手数据。
- 导入预览显示：
  - 文件名、总行数、将导入数量。
  - 重复游戏ID数量。
  - 缺字段数量。
  - 非法评分数量。
  - 将导入前 10 名选手。
  - 跳过前 10 条原因。
- 导入校验增强：
  - 选手名称、游戏ID、偏好位置、评分为必填。
  - 评分必须是 0-120 的有效数字。
  - 游戏ID会与现有选手和本次文件内部数据同时去重。
- 移除旧的直接导入兼容入口，避免绕过预览确认流程。
- 回归测试覆盖：
  - 导入前不会直接写入选手库。
  - 重复ID、缺字段、非法评分统计正确。
  - 确认后才导入有效选手。
  - 50人测试表格导入同样经过预览确认。

### 验证

- `npm test` 通过，输出 `regression ok: 17 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- 内置浏览器验证 `http://127.0.0.1:4176/`：
  - 页面基础交互可用。
  - 当前服务已加载包含 `readPlayerImportPreview`、`confirmPlayerImport`、`playerImportPreviewModal` 的最新源码。
  - 浏览器日志无 error/warn。
- Codex Security 审查：
  - 导入文件大小仍限制为 2MB。
  - 预览弹窗所有文件名、选手名、原因文本均经过 `escapeHtml`。
  - 未发现可利用的 XSS、路径或文件读取风险。

## 2026-05-22：卡池算法可视化

### 已完成

- 选手库普通卡池统计增加评分区间展示。
- 每张选手卡增加系统分池原因：
  - 队长显示“队长专属：已从普通卡池移出”。
  - 普通选手显示评分排名、总人数和四等分段。
- 分池说明继续与实际排序逻辑保持一致：非队长选手按评分从高到低排序，再四等分到猛犸、上等马、中等马、侏儒马。
- 回归测试增加卡池评分区间和系统分池原因断言。

### 验证

- `npm test` 通过，输出 `regression ok: 17 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- 内置浏览器验证 `http://127.0.0.1:4176/`：
  - 选手库可显示系统分池原因。
  - 卡池统计可显示评分区间。
  - 浏览器日志无 error/warn。
- Codex Security 审查：
  - 新增文案均来自内部计算结果或已归一化选手字段。
  - 涉及选手名称、卡池名称的输出仍经过 `escapeHtml`。
  - 未发现新增可利用风险。

## 2026-05-22：阵营锁定 10 队金币商店模式第一轮实施

### 已完成

- 按 `docs/14_阵营锁定10队金币商店模式_实施规范.md` 实施第一轮规则改造。
- 数据结构新增阵营字段：
  - `camp: local` 表示本地人。
  - `camp: outsider` 表示外地人。
- 示例数据调整为：
  - 总人数 50。
  - 本地人 25。
  - 外地人 25。
  - 10 名队长绑定选手。
  - 本地队长 5。
  - 外地队长 5。
- 费用池调整为阵营内独立分费：
  - 每个阵营 1-5 费各 5 人。
  - 队长保留在自己阵营、自己费用池中展示。
  - 队长显示 `队长锁定`，不可作为队员抽取、购买、补位或海克斯目标。
- 商店生成调整：
  - 只读取当前队长同阵营可抽池。
  - 当前商店内不重复展示同一选手。
  - 当某费用没有同阵营可抽选手时，移除该费用并按剩余费用原始概率等比放大。
- 入队与购买调整：
  - `assignment-engine` 增加 `player.camp === captainCamp(captainId)` 硬校验。
  - 购买异阵营选手会拒绝并写入事件日志。
  - 队长锁定选手不可作为队员入队。
  - 最终补位仅从同阵营、非队长、未入队、未禁用选手中随机。
- 海克斯调整：
  - 示例海克斯替换为 10 款新海克斯。
  - 旧海克斯在阵营锁定模式下不再执行。
  - 新海克斯均接入阵营限制，不绕过入队层校验。
- 选手库 UI 调整：
  - 改为本地人卡池和外地人卡池双卡池展示。
  - 每个阵营卡池支持单独筛选全部、1费、2费、3费、4费、5费。
  - 选手卡显示姓名、游戏 ID、阵营、费用、评分和状态。
- 导入调整：
  - JSON/CSV 选手导入识别 `camp`、`阵营` 字段。
  - 支持 `本地人`、`外地人`、`local`、`outsider`。
  - 无阵营或阵营无法识别时导入失败，不静默默认。
- 回归测试重写为阵营锁定模式验收：
  - 阵营人数与队长人数。
  - 本地/外地商店隔离。
  - 异阵营购买拒绝。
  - 最终补位不跨阵营。
  - 队长仍在费用池显示队长锁定。
  - 空费用等比放大。
  - 10 款新海克斯成功路径与失败路径。
  - 导入阵营字段。

### 验证

- `npm test` 通过，输出 `regression ok: 7 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- `npm run camp-loop -- --status=incomplete` 通过。
- `npm run post-task -- --status=incomplete` 通过。
- 本地服务 `http://127.0.0.1:4176/` 可通过 `Invoke-WebRequest` 返回 HTTP 200。
- Codex Security 差异审查：
  - 导入文件仍有 2MB 限制。
  - 新增阵营字段、选手名、游戏 ID、海克斯文案在 UI 输出中继续使用 `escapeHtml` 或 `safeJsonString`。
  - 未发现新增 XSS、路径穿越、任意文件读取或远程代码执行风险。

### 未完成

- 内置浏览器截图验收未完成。
- 阻断原因：Browser 插件访问 `http://127.0.0.1:4176/` 被企业网络策略拒绝，提示不能访问本地地址。
- 已按 Browser 技能要求停止浏览器验证，没有使用 Playwright、CDP 或外部浏览器绕过。
- 因该项阻断，当前循环状态保持 `incomplete`，暂不提交和推送。

## 2026-05-22：知识来源于分解海克斯落地

### 已完成

- 新增金币商店海克斯【知识来源于分解】。
- 每个持有者选人阶段开始时自动获得 1 层解构，最多 3 层；同一轮只触发一次。
- 满 3 层后，海克斯执行队列显示可执行状态。
- 裁判可在目标面板中选择自选目标：
  - 优先显示同阵营 4/5 费可选选手。
  - 若无 4/5 费，自动顺延到 3 费，再顺延到 2 费。
- 目标正常支付费用并消耗本轮购买权。
- 金币不足时，可选择分解本队 2/3 费队员抵扣费用：
  - 2 费抵 2 金币。
  - 3 费抵 3 金币。
  - 被分解选手回到同阵营可选池。
  - 队长不可分解。
- 发动成功后清空 3 层解构，并自动切换下一位队长。
- 状态归一化补充保存：
  - `hexcoreEconomy.decomposeKnowledgeStacks`
  - `wiseBenevolenceRefreshCredits`
  - `donationApplied`
  - `openFeastApplied`
  - `sponsorFlowUsed`
  - `giantSlayerDiscountUsed`
  - 轮次内海克斯触发标记。

### 验证

- `npm test` 通过，输出 `regression ok: 16 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- `npm run camp-loop -- --status=complete` 通过本地门禁。
- 内置浏览器验证 `http://127.0.0.1:4176/`：
  - 页面标题为 `HEXCORE 2.0 裁判控制台`。
  - 海克斯库页面可打开。
  - DOM 中可检索到【知识来源于分解】及解构规则文案。
  - 浏览器 console 无 error/warn。
- Codex Security 差异审查：
  - 新增 UI 选项文本均来自已归一化选手字段，并经过 `escapeHtml`。
  - 新增海克斯仍复用同阵营、队长锁定、队伍容量和金币权限校验。
  - 未发现新增可利用安全问题。

## 2026-05-22：和我困在一起海克斯落地

### 已完成

- 新增金币商店海克斯【和我困在一起】。
- 使用条件：
  - 只能使用 1 次。
  - 只能指定同阵营、可选、非队长锁定选手。
  - 最后一轮不可使用，因为没有下一轮结算窗口。
- 使用后写入运行时延迟效果：
  - 记录使用者。
  - 记录目标选手。
  - 记录下一轮结算轮次。
- 到使用者下一轮选人开始时：
  - 如果目标仍在可选池，系统直接加入该队伍。
  - 该效果消耗本轮购买权。
  - 不扣金币。
  - 若目标已被买走或不可选，效果失效并写入事件日志。
- 实时抽选页在裁判点击开店前会先结算该延迟效果，避免和商店抽卡冲突。
- 海克斯目标弹窗新增锁定选手下拉框。

### 验证

- `npm test` 通过，输出 `regression ok: 16 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- 内置浏览器刷新验证 `http://127.0.0.1:4176/`：
  - 海克斯库可正常渲染。
  - DOM 中可检索到【和我困在一起】。
  - 浏览器 console 无 error/warn。
- Codex Security 差异审查：
  - 目标选手选择仍限制在 `availableCampPlayers`，不会跨阵营。
  - 入队仍经过 `assignment-engine.assign` 的同阵营、队长锁定、容量硬校验。
  - 新增 UI 文本继续使用 `escapeHtml` 或 `safeJsonString`。
  - 未发现新增可利用安全问题。

## 2026-05-23：骤雨 血雾 清风海克斯落地

### 已完成

- 新增金币商店海克斯【骤雨 血雾 清风】。
- 使用流程：
  - 裁判选择一名当前顺位之后的目标队长。
  - 从该目标开始，向后影响最多 3 名队长。
  - 使用者自己不会被影响；若链路中遇到使用者，会自动跳过并顺延。
- 受影响队长下一次生成商店时自动消费天气迷雾效果。
- 天气迷雾商店卡：
  - 隐藏选手姓名、ID、阵营和绝活英雄等信息。
  - 显示“云雾遮蔽”“购买后揭示真实选手”。
  - 使用流动态云雾视觉层。
- 购买逻辑保持不变：
  - 卡牌内部仍绑定真实选手。
  - 购买后按真实选手入队。
  - 仍走金币、阵营、队长锁定和容量校验。

### 验证

- `npm test` 通过，输出 `regression ok: 16 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- 内置浏览器验证 `http://127.0.0.1:4176/`：
  - 页面可正常渲染。
  - DOM 中可检索到【骤雨 血雾 清风】。
  - DOM 中可检索到“天气迷雾”相关内容。
  - 浏览器 console 无 error/warn。
- Codex Security 差异审查：
  - 新增目标链只从当前顺位队列和已归一化队长对象中生成。
  - 新增商店卡文本继续使用 `escapeHtml`。
  - 购买仍使用真实 `playerId` 进入 `assignment-engine` 统一校验。
  - 未发现新增可利用安全问题。

## 2026-05-23：大炮已充能海克斯落地

### 已完成

- 新增金币商店海克斯【大炮已充能】。
- 支持转换技：
  - 雷霆一击：选择一名本轮尚未行动且未满员队长，使其本轮顺位延后一位。
  - 加速之门：使使用者本轮顺位前移一位，并保持当前操作队长不丢失。
- 每轮最多使用 1 次，跨轮可再次使用。
- 顺位变更通过 `runtimeEffects` 写入，随后调用顺位引擎重算，顺位解释会显示来源。
- 目标选择弹窗已接入裁判端海克斯执行队列。

### 验证

- 新增回归覆盖：
  - 雷霆一击目标后移一位。
  - 同轮重复使用被拒绝。
  - 加速之门使用者前移一位且 `currentIndex` 仍指向使用者。

## 2026-05-23：神兵天降海克斯落地

### 已完成

- 新增金币商店海克斯【神兵天降】。
- 任意队长购买成功后，如果场上存在未使用的【神兵天降】，实时抽选页顶部显示 10 秒发动窗口。
- 发动效果：
  - 将刚购买的选手移回可选池。
  - 返还目标队长本次实际支付费用。
  - 将目标队长追加到本轮末尾补偿回合。
  - 【神兵天降】标记为已使用。
- 点击“下一位”会直接关闭发动窗口并继续推进流程，相当于跳过等待。
- 补偿回合进入时重置目标队长本轮购买权和免费开店状态，允许重新开店购买。

### 验证

- 新增回归覆盖：
  - 购买成功后出现神兵天降发动窗口。
  - 发动后退回刚购买选手并恢复可选状态。
  - 发动后返还金币。
  - 目标队长被追加到本轮末尾。
  - 补偿回合允许目标队长重新开店。

## 2026-05-23：神秘贤者·盲盒金币版落地

### 已完成

- 新增金币商店海克斯【神秘贤者·盲盒】。
- 使用条件：
  - 当前队长未打开商店。
  - 当前队长本轮未购买、未跳过、队伍未满。
  - 当前队长金币不少于 3。
  - 同阵营存在 2-5 费可选选手。
- 执行效果：
  - 固定支付 3 金币。
  - 从同阵营 2-5 费可选选手中随机盲抽 1 人加入队伍。
  - 消耗本轮购买权。
  - 每局 1 次，使用后标记为已使用。

### 验证

- 新增回归覆盖：
  - 盲盒可在满足条件时执行。
  - 固定扣除 3 金币。
  - 入队目标来自同阵营 2-5 费可选池。
  - 执行后消耗本轮购买权并标记海克斯已使用。

## 2026-05-23：海克斯五抽一与舞会女王落地

### 已完成

- 队长抽海克斯流程改为五抽一：
  - 当前队长最多抽出 5 个全局未占用候选。
  - 每次会话最多刷新其中 1 张。
  - 最多选择 1 个海克斯，选择后会话关闭。
  - 选择后不自动切换队长，仍由裁判点击“下一位”。
- 海克斯候选增加全局唯一约束：
  - 已被其他队长选择的海克斯不会进入候选。
  - 裁判兜底分配会校验全局占用。
  - 移除海克斯后释放占用，可重新进入候选池。
- 新增金币商店海克斯【舞会女王】：
  - 高费可选数量足够时，本轮商店只抽 3-5 费卡。
  - 高费不足以填满商店时，先抽高费，再降级补足，并写入生效原因。

### 验证

- `npm test` 通过，输出 `regression ok: 19 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- 内置浏览器使用 `http://localhost:4176/?qa=hex-five` 隔离源验证：
  - 页面标题为 `HEXCORE 2.0 裁判控制台`。
  - 海克斯库可显示五抽一文案和【舞会女王】。
  - 点击抽取后生成 5 张候选。
  - 点击“刷新此张”后仍保持 5 张候选，并显示刷新已用。
  - 点击“选择此海克斯”后候选区关闭，不自动生成下一组。
  - 浏览器 console 无 error/warn。
- Codex Security 差异审查：
  - 本次改动未新增网络、文件、命令执行、动态脚本执行或跨源数据传输入口。
  - 新增 UI 文本继续走 `escapeHtml` / `safeJsonString`。
  - 海克斯选择和兜底分配在写入前均校验全局占用，未发现越权覆盖其他队长海克斯的新增路径。
  - 舞会女王只改变商店候选筛选和事件原因，不绕过购买、阵营、队长、容量和金币校验。
  - 未发现新增可利用安全问题。

## 2026-05-23：神秘贤者·启元落地

### 已完成

- 新增金币商店海克斯【神秘贤者·启元】：
  - 获得时为持有队长初始资金 +2。
  - 每轮最多可使用 1 次。
  - 商店打开前可发动，将使用者提到本轮第一顺位，原第一及后续队长顺延。
  - 顺位修正优先级高于大炮已充能的顺位微调。
  - 商店已打开、购买权已处理、不在本轮顺位内、行动窗口已过或已经是第一顺位时不可执行，并给出明确原因。

### 验证

- 新增回归覆盖：
  - 获得启元时金币 +2。
  - 发动后使用者进入本轮第一顺位。
  - 发动后当前索引仍指向使用者。
  - 同轮再次发动失败。
- `npm test` 通过，输出 `regression ok: 19 tests`。
- `git diff --check -- src\core\sample-data.js src\main.js src\engines\hexcore-engine.js scripts\regression.js docs\06_开发计划.md docs\09_执行记录.md` 通过，仅有 CRLF 提示。
- 使用 Build Web Apps 前端调试流程和内置 Browser 验证：
  - 打开 `http://localhost:4176/?qa=origin-sage`。
  - 页面标题为 `HEXCORE 2.0 裁判控制台`。
  - DOM 快照包含 `HEXCORE 2.0`，页面非空。
  - 点击侧边栏“海克斯库”后，页面显示“海克斯库”“神秘贤者·启元”和五抽一文案。
  - 浏览器 console 无应用侧 error/warn。
  - 内置浏览器截图接口连续返回 `Page.captureScreenshot` 超时，未取得截图证据；本项以 DOM 和交互状态作为烟测证据。
- Codex Security 差异审查：
  - 本次新增启元只写入本地内存状态、事件日志和回归断言，未新增网络、文件、命令执行、动态脚本执行或跨源数据传输入口。
  - UI 输出仍经现有 `escapeHtml` 渲染路径；新增中文文案无 HTML 注入面。
  - 启元顺位效果走既有 `runtimeEffects -> turnOrderEngine.recompute()` 通道，不绕过队伍容量、队长锁定、阵营购买或金币校验。
  - 获得时金币 +2 使用独立 `originSageBonusApplied` 标记，避免重复分配同一海克斯时重复加钱。
  - 未发现新增可利用安全问题。

## 2026-05-23：雪定饿的喵落地

### 已完成

- 新增金币商店海克斯【雪定饿的喵】：
  - 可对任意非自己的未满员队长使用。
  - 目标下一次商店生成后，将卡牌显示身份按卡槽轮转打乱。
  - 卡片展示显示身份和显示阵营标记，费用不参与打乱。
  - 购买时按真实卡位费用扣款，真实选手入队。
  - 购买后写入“雪定饿的喵揭示”日志，记录显示卡、真实卡和扣款。
- 新增商店样式：
  - 受影响卡片显示“信息扰乱”标记。

### 验证

- 新增回归覆盖：
  - 雪猫可对目标队长使用。
  - 目标开店时消费 `snow_cat_shuffle` 效果。
  - 商店卡存在 `displayPlayerId`，且至少一张卡显示身份与真实身份不同。
  - UI 渲染包含 `snow-cat-card` 和“信息扰乱”。
  - 购买后真实选手入队。
  - 扣款等于真实卡位费用。
  - 事件日志写入购买揭示。
- `npm test` 通过，输出 `regression ok: 19 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- 使用 Build Web Apps 前端调试流程和内置 Browser 验证：
  - 打开 `http://localhost:4176/?qa=snow-cat`。
  - 页面标题为 `HEXCORE 2.0 裁判控制台`。
  - DOM 快照包含 `HEXCORE 2.0`，页面非空。
  - 点击侧边栏“海克斯库”后，页面显示“海克斯库”和“雪定饿的喵”。
  - 浏览器 console 无应用侧 error/warn。
- Codex Security 差异审查：
  - 雪猫只在本地状态中新增 `displayPlayerId`、`snowCatShuffled` 和运行时效果，不新增外部输入、网络、文件或脚本执行入口。
  - 购买仍走 `assignmentEngine.purchase` 的阵营、队长锁定、容量、金币校验。
  - 真实卡位费用通过 `basePriceOverride` 进入既有购买定价流程，仍会触发金币不足校验和购买权固化。
  - UI 新增文本继续走 `escapeHtml`；未发现新增可利用安全问题。

## 2026-05-23：海浪，我没吃饭落地

### 已完成

- 新增金币商店海克斯【海浪，我没吃饭】：
  - 持有者进入自己的选人阶段时，自动放弃主动购买。
  - 系统登记本轮等待夺取效果，并立即进入下一位队长。
  - 若持有者是本轮第一顺位，会等待下一名其他队长购买。
  - 下一名其他队长购买成功后，选手从购买队长队伍转入海浪持有者队伍。
  - 原购买队长获得本次购买费用返还。
  - 若持有者队伍已满，夺取失败并写入日志。

### 验证

- 新增回归覆盖：
  - 持有者第一顺位时不会开店，自动进入下一位。
  - 运行时效果登记为等待夺取。
  - 下一名其他队长购买后，真实选手归属转移到海浪持有者。
  - 原购买队长队伍不再保留该选手。
  - 原购买队长金币恢复到购买前。
  - 选手 `teamId` 更新为海浪持有者。
- `npm test` 通过，输出 `regression ok: 19 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- 使用 Build Web Apps 前端调试流程和内置 Browser 验证：
  - 打开 `http://localhost:4176/?qa=hungry-wave`。
  - 页面标题为 `HEXCORE 2.0 裁判控制台`。
  - DOM 快照包含 `HEXCORE 2.0`，页面非空。
  - 点击侧边栏“海克斯库”后，页面显示“海克斯库”和“海浪，我没吃饭”。
  - 浏览器 console 无应用侧 error/warn。
- Codex Security 差异审查：
  - 海浪只在本地状态内转移已购买选手归属并返还金币，不新增外部输入、网络、文件、命令或脚本执行入口。
  - 夺取发生在原购买流程完成之后，仍复用既有购买金币校验、购买权固化和选手有效性校验。
  - 转移前校验原购买队长仍持有该选手，防止状态变化后错误转移。
  - 队伍满员时不转移并消费等待效果，避免超员。
  - 未发现新增可利用安全问题。

## 2026-05-23：质变黄金阶与棱彩阶落地

### 已完成

- 新增金币商店海克斯【质变：黄金阶】：
  - 商店打开前免费发动。
  - 从同阵营 4 费可选池随机获得 1 人。
  - 成功后消耗本轮购买权。
- 新增金币商店海克斯【质变：棱彩阶】：
  - 商店打开前免费发动。
  - 从同阵营 5 费可选池随机获得 1 人。
  - 成功后消耗本轮购买权。
- 目标池为空、商店已打开或购买权已处理时执行失败，并保留购买权。

### 验证

- 新增回归覆盖：
  - 黄金阶从同阵营 4 费可选池入队。
  - 棱彩阶从同阵营 5 费可选池入队。
  - 成功后消耗本轮购买权。
  - 目标池为空时失败且不消耗购买权。
- `npm test` 通过，输出 `regression ok: 19 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- 使用 Build Web Apps 前端调试流程和内置 Browser 验证：
  - 打开 `http://localhost:4176/?qa=transmute`。
  - 页面标题为 `HEXCORE 2.0 裁判控制台`。
  - DOM 快照包含 `HEXCORE 2.0`，页面非空。
  - 点击侧边栏“海克斯库”后，页面显示“质变：黄金阶”和“质变：棱彩阶”。
  - 浏览器 console 无应用侧 error/warn。
- Codex Security 差异审查：
  - 质变只从本地同阵营可选池中随机选择，未新增外部输入、网络、文件、命令或脚本执行入口。
  - 入队仍走 `assignmentEngine.assign`，保留阵营、队长锁定和容量校验。
  - 失败路径在写入购买权前返回，不会造成误消耗。
  - 未发现新增可利用安全问题。

## 2026-05-23：稳健补强重做落地

### 已完成

- 重做金币商店海克斯【稳健补强】：
  - 商店打开前使用。
  - 第1-2轮至少从2费池开始补强。
  - 第3轮至少从3费池开始补强。
  - 第4轮至少从4费池开始补强。
  - 从符合条件的最低可用费用池随机分配 1 人。
  - 免费但消耗本轮购买权。
  - 目标池为空时失败并给出明确原因。

### 验证

- 新增/更新回归覆盖：
  - 第1轮稳健补强成功入队。
  - 入队选手费用至少为2费。
  - 成功后消耗本轮购买权。
- `npm test` 通过，输出 `regression ok: 19 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- 使用 Build Web Apps 前端调试流程和内置 Browser 验证：
  - 打开 `http://localhost:4176/?qa=steady`。
  - 页面标题为 `HEXCORE 2.0 裁判控制台`。
  - DOM 快照包含 `HEXCORE 2.0`，页面非空。
  - 点击侧边栏“海克斯库”后，页面显示“稳健补强”和“第1-2轮至少2费”规则文案。
  - 浏览器 console 无应用侧 error/warn。
- Codex Security 差异审查：
  - 稳健补强仍只从本地同阵营可选池中随机选择，未新增外部输入、网络、文件、命令或脚本执行入口。
  - 入队仍走 `assignmentEngine.assign`，保留阵营、队长锁定和容量校验。
  - 成功后才写入购买权，失败不会误消耗。
  - 未发现新增可利用安全问题。

## 2026-05-23：背水一战落地

### 已完成

- 新增金币商店海克斯【背水一战】：
  - 当前队伍已有 4 名队员时才可使用。
  - 使用者放弃原 4 名队员。
  - 从全体非队长、非禁用、非原队员选手中随机获得 4 人。
  - 若抽到其他队伍已有队员，则该队伍从使用者原队员中获得 1 人作为置换补偿。
  - 成功后只标记海克斯已使用，不消耗购买权；队伍满 4 人时购买权自然失效。
  - 日志记录换入选手、原队员和每次补偿。

### 验证

- 新增回归覆盖：
  - 满 4 名队员时可以发动。
  - 使用者换入 4 名非队长选手。
  - 原队员不再留在使用者队伍。
  - 被抽走队员的队伍获得 1 名原队员补偿。
  - 被抽走选手归属更新为使用者。
  - 成功后消耗本轮购买权。
- `npm test` 通过，输出 `regression ok: 19 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- 使用 Build Web Apps 前端调试流程和内置 Browser 验证：
  - 打开 `http://localhost:4176/?qa=last-stand`。
  - 页面标题为 `HEXCORE 2.0 裁判控制台`。
  - DOM 快照包含 `HEXCORE 2.0`，页面非空。
  - 点击侧边栏“海克斯库”后，页面显示“背水一战”。
  - 浏览器 console 无应用侧 error/warn。
- Codex Security 差异审查：
  - 背水一战仅在本地状态内重排选手归属，未新增外部输入、网络、文件、命令或脚本执行入口。
  - 执行前校验当前队伍有 4 名队员、候选池至少 4 人，避免半成品状态。
  - 转移时先移除旧归属再写入新归属，补偿队伍使用原队员队列，避免重复归属。
  - 成功后消耗购买权，失败不写入购买权。
  - 未发现新增可利用安全问题。

## 2026-05-23：导入校验增强落地

### 已完成

- 补齐选手导入预览统计：
  - 显示总行数、将导入、跳过总数。
  - 单独显示重复 ID、缺字段、非法评分、阵营缺失。
  - 预览有效行展示阵营，便于裁判确认导入数据。
- 缺少或无法识别阵营的行进入跳过列表，不会中断整批预览。
- 仍保留严格解析接口：直接解析缺阵营数据时继续报错，避免绕过数据完整性要求。

### 验证

- 更新回归覆盖：
  - 导入预览统计总行数、跳过总数。
  - 阵营缺失、非法评分、重复 ID 分别计数。
  - 确认导入前不写入选手库。
  - 点击确认后只写入有效选手。
- `npm test` 通过，输出 `regression ok: 19 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- 使用 Build Web Apps 前端调试流程和内置 Browser 验证：
  - 打开 `http://localhost:4176/?qa=import-preview`。
  - 页面标题为 `HEXCORE 2.0 裁判控制台`。
  - DOM 快照包含 `HEXCORE 2.0`，页面非空。
  - 点击侧边栏“选手库”可正常切换页面。
  - 浏览器 console 无应用侧 error/warn。
- Codex Security 差异审查：
  - 导入仍只读取本地 JSON/CSV 文件内容，未新增网络、命令、脚本执行或远程加载入口。
  - 预览和渲染继续使用 `escapeHtml`，导入文本不会作为 HTML 执行。
  - 确认前不写入状态，跳过项不会进入选手库。
  - 未发现新增可利用安全问题。

## 2026-05-23：队伍异常识别增强落地

### 已完成

- 队伍管理页补充异常识别：
  - 超员。
  - 缺员。
  - 满员但未设置队长。
  - 缺失选手。
  - 禁用选手仍在队内。
  - 重复归属。
  - 队员归属指向其他队伍。
- 可自动处理的问题显示“修复异常”：
  - 清理缺失成员。
  - 清理禁用成员。
  - 清理重复成员。
  - 清理超出容量的成员。
  - 修正保留成员的队伍归属。
- 不自动猜测队长；未设置队长时只提示裁判在队员卡片点击“设为队长”。

### 验证

- 新增回归覆盖：
  - 队伍页显示重复归属、缺失选手、禁用选手。
  - 存在可修复项时显示修复入口。
  - 修复后只保留有效成员。
  - 禁用选手不会被错误恢复为可选。
- `npm test` 通过，输出 `regression ok: 20 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- 使用 Build Web Apps 前端调试流程和内置 Browser 验证：
  - 打开 `http://localhost:4176/?qa=team-issues`。
  - 页面标题为 `HEXCORE 2.0 裁判控制台`。
  - 点击侧边栏“队伍管理”后页面显示队伍状态。
  - 浏览器 console 无应用侧 error/warn。
- Codex Security 差异审查：
  - 修复动作只操作本地内存状态和本地存储，未新增外部输入、网络、文件、命令或脚本执行入口。
  - 修复逻辑只删除无效、重复、禁用和超员成员，不自动分配队长或新增选手，避免误授权。
  - 抽选进行中、已有商店或流程完成时拒绝自动修复，降低临场状态被破坏风险。
  - 未发现新增可利用安全问题。

## 2026-05-23：卡池解释增强落地

### 已完成

- 选手卡片补充费用池解释：
  - 显示阵营内评分排名。
  - 显示当前费用。
  - 显示当前费用池边界。
  - 普通选手说明“队长已从普通池剔除后按剩余人数五档重分”。
  - 队长锁定卡说明“不参与普通费用池重算”。
- 普通费用池重算逻辑同步调整为剔除队长后按剩余人数五档分配，避免队长占用普通抽选池名额。

### 验证

- 更新回归覆盖：
  - 选手库渲染费用池边界。
  - 选手卡展示队长剔除后重分池说明。
- `npm test` 通过，输出 `regression ok: 20 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- 使用 Build Web Apps 前端调试流程和内置 Browser 验证：
  - 打开 `http://127.0.0.1:4176/?qa=pool-explain`。
  - 页面标题为 `HEXCORE 2.0 裁判控制台`。
  - 点击侧边栏“选手库”后页面显示“费边界”和“队长已从普通池剔除后按剩余人数五档重分”。
  - 浏览器 console 无应用侧 error/warn。
- Codex Security 差异审查：
  - 改动只影响本地分池计算和文本渲染，未新增外部输入、网络、文件、命令或脚本执行入口。
  - 展示文本继续使用 `escapeHtml`。
  - 分池逻辑只读取本地选手和队长状态，不引入权限绕过。
  - 未发现新增可利用安全问题。

## 2026-05-23：状态完整性检查落地

### 已完成

- 系统设置页新增“状态完整性检查”结果面板。
- “运行状态检查”会保留最近检查时间、通过状态和问题列表。
- 检查范围覆盖：
  - 重复归属。
  - 跨阵营。
  - 超员。
  - 队长冲突。
  - 顺位异常。
  - 卡池异常。
  - 缺失选手和不存在队伍引用。

### 验证

- 新增回归覆盖：
  - 重复归属可被识别。
  - 跨阵营可被识别。
  - 顺位异常可被识别。
  - 系统设置页显示检查面板和处理状态。
- `npm test` 通过，输出 `regression ok: 21 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- 使用 Build Web Apps 前端调试流程和内置 Browser 验证：
  - 打开 `http://127.0.0.1:4176/?qa=system-check`。
  - 点击侧边栏“系统设置”。
  - 点击“运行状态检查”后页面显示“状态完整性检查”和最近检查状态。
  - 浏览器 console 无应用侧 error/warn。
- Codex Security 差异审查：
  - 检查逻辑只读取本地状态并渲染问题列表，未新增外部输入、网络、文件、命令或脚本执行入口。
  - 检查结果文本继续使用 `escapeHtml`。
  - 不自动修复状态，避免检查动作造成隐藏副作用。
  - 未发现新增可利用安全问题。

## 2026-05-24：开发计划闭环与海克斯图标资产体系

### 已完成

- 海克斯 PNG 图标资产体系闭环：
  - `public/assets/hex-icons/` 已包含当前金币模式 27 个海克斯 PNG。
  - 海克斯候选卡、海克斯库、已拥有海克斯和详情弹窗统一使用 PNG 优先图标组件。
  - PNG 缺失或加载失败时自动回退到原 SVG 图标。
- 本地静态服务支持 `/assets/...` 映射到 `public/assets/...`，并保留根目录路径穿越校验。
- 后置钩子误判修正：
  - 不再把已完成验收文案中的“失败原因”“风险属性”等词误判为未完成线索。
  - 不再把“场景”表格当成状态表格。
- 开发计划文档同步：
  - 海克斯 PNG 图标资产体系标记为已完成。
  - 实时抽选队伍阵容看板、选手卡直接点击购买在后续执行顺序中标记为已完成。
  - 回归测试数量更新为 36 项。

### 验证

- `npm test` 通过，输出 `regression ok: 36 tests`。
- `git diff --check` 通过，仅有 CRLF 提示。
- `npm run post-task -- --status=complete --doc=docs/06_开发计划.md` 通过，后置钩子清零 incomplete 计数。
- 使用 Build Web Apps 前端调试流程判断验证边界：
  - 本轮未新增渲染 UI，因此未追加浏览器截图。
  - 上一轮已用 Edge 验证海克斯 PNG 图标在候选卡、海克斯库和详情弹窗正常加载。
  - 缺图场景已验证 SVG 兜底正常显示。

### 安全审查结论

- 本轮只更新文档记录和后置钩子识别规则，未新增外部输入、网络请求、任意文件写入、命令执行或鉴权入口。
- `/assets/...` 静态服务映射仍经过 `path.resolve` 与 `path.relative` 根目录约束；回归覆盖 assets 映射、路径穿越和非法 URL 编码。
- 未发现新增可利用安全问题。

### 剩余风险

- 当前工作树仍有未提交改动和未跟踪 PNG 资产。
- 推送前需确认 `public/assets/hex-icons/` 进入版本控制，并确认 `tmp/` 是否为临时目录、不应提交。

## 2026-05-27：多人端返回房间入口修正

### 已完成

- 修正多人端进入裁判端后无法回到多人房间的问题。
- 顶栏新增“返回多人房间”按钮，裁判端、队长端和观众端均可使用。
- 点击返回时清理本机多人 session，关闭 SSE 实时同步连接，移除 URL 角色参数，并重新渲染多人房间创建/加入页。
- 将项目版本提升为 `2.0.5`，同步 `package.json`、运行界面版本元信息、回归断言和 `CHANGELOG.md`。

### 验证

- 回归新增多人端返回入口断言，覆盖按钮、session 清理、同步连接关闭和 URL 角色参数移除。
- `npm test` 通过，输出 `regression ok: 56 tests`。
- `git diff --check` 通过，仅有 Git 行尾转换提示。
- 内置 Browser 验证 `http://127.0.0.1:4186/`：创建赛事后进入 `?role=referee`，顶栏出现“返回多人房间”；点击后 URL 回到 `/`，重新显示多人房间创建/加入页，控制台无 error/warn。
- 内置 Browser 截图接口在本次环境中多次返回 `Page.captureScreenshot` 超时，已保留 URL、DOM 快照和 console 结果作为交互证据。
- 安全复核：本次只新增本机会话清理、SSE 关闭和同源 URL 参数移除，不新增外部输入解析、鉴权放宽、任意文件写入、命令执行或跨域发送；差异范围未发现新的可利用入口。

## 2026-05-25：背水一战同阵营置换修正

### 已完成

- 修正 `背水一战` 金币模式规则：
  - 发动条件改为当前队长已有 4 名队员，且本轮未购买、未跳过。
  - 候选池限定为本阵营、非禁用、非队长、非当前 4 名队员。
  - 候选可以来自可选池，也可以来自本阵营其他队伍。
  - 抽中其他队伍队员时，该队从发动者原 4 名队员中随机获得 1 名补偿。
  - 抽中可选池选手时不置换，未补偿原队员回到可选池。
- 移除 `背水一战` 第 4 轮自动提到第一顺位的旧逻辑。
- 新增 `背水一战` 主题化确认弹窗：
  - 点击执行队列或可用海克斯横幅时先打开确认弹窗。
  - 弹窗展示当前 4 人、本阵营候选数、可能触发置换数和候选预览。
  - 确认前不改变队伍状态，确认后进入入队揭示弹窗。
- 文档同步：
  - 更新 `docs/06_开发计划.md` 开发手册。
  - 更新 `docs/14_阵营锁定10队金币商店模式_实施规范.md` 规则口径。
  - 更新 `docs/05_海克斯引擎设计.md`，删除旧顺位冲突说明。

### 验证

- 新增回归覆盖：
  - 本阵营候选不足 4 人时，不能用异阵营候选凑数。
  - UI 执行 `背水一战` 先打开确认弹窗，确认前不改变阵容。
  - 确认后只换入本阵营队员，异阵营候选不会进入队伍。
  - 抽中别队队员时完成原队员随机补偿，未补偿原队员回到可选池。
  - 本轮已购买后不能再发动。
  - 第 4 轮不再出现 `背水一战` 顺位解释。
  - `last-stand-modal` 纳入主题弹窗和统一按钮样式断言。
- `npm test` 通过，输出 `regression ok: 43 tests`。

## 2026-05-25：最新海克斯介绍指南更新

### 已完成

- 更新 `docs/HEXCORE2.0_当前海克斯详细介绍.docx`：
  - 更新日期改为 2026-05-25。
  - 同步 `海浪，我没吃饭`、`背水一战`、`神秘贤者·启元`、`骤雨 血雾 清风`、`雪定饿的喵`、`大炮已充能`、`神兵天降` 的最新运行口径。
  - 补充雪定饿的喵与加急调货叠加、海浪与雷霆/血雾、背水与海浪队伍阵容的冲突边界。
- 更新 `docs/海克斯介绍以及攻略文档.docx`：
  - 同步总览表、单卡说明、组合建议和队长临场检查清单。
  - 清除旧口径：显示费用结算、神兵天降补偿回合、启元手动选择、背水跨阵营随机等描述。
- 对两份 Word 文档的表格行增加“不跨页拆分”，避免渲染时出现表格行残片。

### 验证

- 使用 Word 后台导出 PDF，并用 Python 渲染为 PNG 页面进行视觉检查：
  - `HEXCORE2.0_当前海克斯详细介绍.docx`：18 页。
  - `海克斯介绍以及攻略文档.docx`：26 页。
- 关键词结构检查通过：
  - 不再包含 `显示费用`、`费用按显示`、`本轮末尾补偿回合`、`每轮可选择是否提到第一顺位` 等旧口径。

## 2026-05-25：海克斯组合冲突复查

### 已完成

- 复查顺位、商店扰乱、入队置换三类组合边界，并修正 3 个冲突点：
  - `大炮已充能 / 雷霆一击`：目标列表排除持有 `海浪，我没吃饭` 的队长，即使本轮尚未触发海浪。
  - `背水一战`：候选池排除持有 `海浪，我没吃饭` 队伍当前拥有的队员，避免抽走海浪免疫队伍阵容。
  - `雪定饿的喵 + 加急调货`：调货替换真实卡后重新映射显示身份，显示身份只来自当前商店卡。
- 同步更新大炮弹窗文案、雪定饿的喵费用说明、开发手册和金币商店实施规范。

### 验证

- 新增回归覆盖：
  - 雪定饿的喵影响的商店使用加急调货后，显示身份集合仍等于当前商店真实卡集合。
  - 持有海浪我没吃饭的队长不会出现在雷霆一击目标列表。
  - 背水一战候选不包含海浪我没吃饭队伍当前拥有的队员。
- `npm test` 通过，输出 `regression ok: 43 tests`。
